## 1. Project Setup

- [x] 1.1 Create the `.slnx` solution at the repo root
- [x] 1.2 **Corrected after a structural review** (see §20 below): the solution has exactly one solution folder, `Tests`; the five architecture projects are flat, unfoldered, at the solution root — no `Domain`/`Application`/`Infrastructure`/`Presentation`/`Host` solution folders. Originally created as five separate solution folders (one per layer); removed once it was pointed out that a wrapping folder duplicating the project's own name is redundant, and doesn't match Ardalis'/Jason Taylor's Clean Architecture reference layouts
- [x] 1.3 Scaffold `src/OutOfTheBox.Domain` (plain `net10.0` class library, no NuGet dependencies beyond the BCL) — **corrected**: originally `src/Domain/OutOfTheBox.Domain/` (double-nested); flattened to sit directly under `src/`
- [x] 1.4 Scaffold `src/OutOfTheBox.Application` (plain `net10.0` class library, references `Domain` only) — same flattening correction as 1.3
- [x] 1.5 Scaffold `src/OutOfTheBox.Infrastructure` (`net10.0-windows` class library, references `Application` + `Domain` only) — same flattening correction as 1.3
- [x] 1.6 Scaffold `src/OutOfTheBox.Presentation` (plain `net10.0` Razor Class Library — `Sdk="Microsoft.NET.Sdk.Razor"` — carrying `.razor` components, `wwwroot`, minimal API endpoint-mapping extension methods, and auth middleware; references `Application` + `Domain` only, **no reference to `Infrastructure`**) — same flattening correction as 1.3
- [x] 1.7 Scaffold `src/OutOfTheBox.Host` (`net10.0-windows` ASP.NET Core executable — the only project referencing all four others) — same flattening correction as 1.3
- [x] 1.8 In `Host`'s `Program.cs`: build the `WebApplication` and bind `ServiceOptions`; a marked placeholder notes where `Presentation`'s endpoint-mapping and `Infrastructure`'s DI registrations land as those pieces are built in later sections; Kestrel/HTTPS hardening deferred to §16 Transport & Network
- [x] 1.9 Add `Microsoft.Extensions.Hosting.WindowsServices` to `Host` and configure `UseWindowsService()`
- [x] 1.10 Add configuration schema (appsettings.json + env var overrides via the default ASP.NET Core config provider chain) for: root directory, bearer token, listen port, default execution timeout (10 minutes), maximum execution timeout, output size cap, SQLite file path — bound to a `ServiceOptions` type
- [x] 1.11 Enable `<Nullable>enable</Nullable>` and `<GenerateDocumentationFile>true</GenerateDocumentationFile>` (with `CS1591` as an error) on every project via a shared `Directory.Build.props`
- [x] 1.12 Add `<Version>1.0.0</Version>` to `Directory.Build.props` (standard SemVer, starting directly at 1.0.0 — no `0.x` pre-release versions, and not bumped incrementally through the rest of this plan, per explicit instruction); confirm every project's assembly picks it up automatically (`AssemblyVersion`/`FileVersion`/`InformationalVersion` all derive from this one property, no per-project override needed)

## 2. Test Suite Foundation

- [x] 2.1 Scaffold `tests/OutOfTheBox.UnitTests` (xUnit, `Microsoft.NET.Test.Sdk`), references `Domain`, `Application`, `Infrastructure` — no real process spawning, no real network; fakes/mocks for `Process`, `IRunEventBus`, `IResourceEventBus`, a real SQLite `:memory:` connection for repository-logic tests, and the internal `Domain`/`Application`/`Infrastructure` folder split all land as each layer gains real test content in later sections. **Corrections**: (a) from design.md — since it references `Infrastructure` (`net10.0-windows`), a plain `net10.0` project can't reference it — `UnitTests` (and `BehaviorTests`/`ArchitectureTests`, both of which reference `Host`/`Infrastructure`) target `net10.0-windows` too, not plain `net10.0`; (b) structural — originally `tests/UnitTests/OutOfTheBox.UnitTests/` (double-nested); flattened to sit directly under `tests/` (see §20)
- [x] 2.2 Scaffold `tests/OutOfTheBox.BehaviorTests` (Reqnroll.xUnit + `Microsoft.AspNetCore.Mvc.Testing`), references `Host`, for Gherkin `.feature`-file scenarios exercised against the real ASP.NET Core pipeline via `WebApplicationFactory<Program>`/`TestServer` — required exposing Host's implicit `Program` class via a trailing `public partial class Program;` so another assembly can reference it; same flattening correction as 2.1
- [x] 2.3 Scaffold `tests/OutOfTheBox.ArchitectureTests` (xUnit + `NetArchTest.Rules`), referencing all five projects (only for reflection over their assemblies, not for calling into them); same flattening correction as 2.1
- [x] 2.4 Write the NetArchTest rules in `LayeringTests.cs`: `Domain` has no dependency on `Application`/`Infrastructure`/`Presentation`/`Host` or on ASP.NET Core/EF Core/`System.Management`/`PerformanceCounter` namespaces; `Application` has no dependency on `Infrastructure`/`Presentation`/`Host`; `Infrastructure` has no dependency on `Presentation`/`Host`; `Presentation` has no dependency on `Infrastructure`/`Host` — no exception for any of these; `Host` is excluded from the rule set entirely (it's the composition root, expected to reference everything)
- [x] 2.5 Added `UnitTests`, `BehaviorTests`, `ArchitectureTests` under the (only) `Tests` solution folder in the `.slnx`, flat (no further sub-grouping by test type). **Deviation**: the `Fixtures` projects are deliberately *not* added to the `.slnx` — they live as plain standalone `.csproj`s under `tests/Fixtures/`, never solution-registered, because a failing/hanging test inside them would otherwise break `dotnet test` on the main solution (they're targets the service spawns `dotnet` against, not part of this repo's own test run)
- [x] 2.6 Added Reqnroll.xUnit and confirmed a trivial scenario (`Sanity.feature`) runs end-to-end via plain `dotnet test` — passed, 1/1
- [x] 2.7 Added `tests/Fixtures/`: `PassingFixture` (one passing test), `FailingFixture` (one deliberately-wrong assertion), `HangingFixture` (one test that awaits `Task.Delay(Timeout.Infinite)` and never returns) — verified each behaves as intended (pass / fail with a clear assertion message / genuinely hangs until killed)
- [ ] 2.8 Write one Gherkin feature file per capability (`dotnet-command-execution.feature`, `git-command-execution.feature`, `artifact-transfer.feature`, `repository-management.feature`, `service-authentication.feature`, `run-history.feature`, `service-dashboard.feature`, `host-resource-monitoring.feature`), with each scenario's Given/When/Then derived directly from that capability's spec.md `#### Scenario:` blocks, so spec and executable test stay in lockstep — **deferred**: per design.md's "land alongside each piece" plan, each capability's feature file is written in its own implementation section (§3, §5/§6/§8, §9, §10, §13, §11/§12), not upfront here, since a feature file with no matching step definitions would sit as an undefined/pending scenario until that capability actually exists
- [x] 2.9 Test convention documented in `design.md`'s "Test project layout" decision (unit tests cover isolated Domain/Application/targeted-Infrastructure logic; BDD feature files cover spec.md scenarios; NetArchTest covers layering; real-deployment checks stay manual in §19)
- [x] 2.10 Confirmed `dotnet build OutOfTheBox.slnx` and `dotnet test OutOfTheBox.slnx` both succeed clean (0 warnings/errors; ArchitectureTests 5/5, BehaviorTests 1/1, UnitTests 0 tests — expected, no test content yet)
- [x] 2.11 Sanity-checked the NetArchTest rules: temporarily referenced a `Presentation` type from `Infrastructure`, confirmed `Infrastructure_has_no_dependency_on_presentation` failed and named the exact offending type, then fully reverted (file removal + `dotnet remove reference`) and reconfirmed a clean 5/5 pass

## 3. Authentication

- [x] 3.1 Implemented `BearerAuthenticationFilter` (`IEndpointFilter`, in `Presentation`) requiring an `Authorization: Bearer <token>` header — built as a reusable filter, not yet attached to the command-execution/cancellation endpoints since those are built in §5/§8 (`.AddEndpointFilter<BearerAuthenticationFilter>()` happens when those endpoints are mapped)
- [x] 3.2 Filter returns `Results.Unauthorized()` and short-circuits (never calls `next`) before any handler runs, for both a missing and a wrong credential
- [x] 3.3 Comparison implemented in `Domain.Authentication.CredentialComparer` via `CryptographicOperations.FixedTimeEquals` — kept in `Domain` since it's pure logic with zero framework dependency, callable from `Presentation`'s filter
- [x] 3.4 Expected credential read from `ServiceOptions.BearerToken`, bound from configuration in `Host`'s `Program.cs` — **correction**: `ServiceOptions` was relocated from `Host` to `Application.Configuration`, since `Presentation` (which needs it for the filter) cannot depend on `Host` per the architecture rule; `Host` still owns binding the actual `IConfiguration` values into it
- [x] 3.5 Unit tests in `CredentialComparerTests.cs`: identical/different/different-length/case-sensitivity/missing-provided/empty-expected — 7 cases, all passing
- [x] 3.6 BDD in `ServiceAuthentication.feature` (4 scenarios: missing/valid/wrong credential + rotation), driving the real filter through `EndpointFilterInvocationContext.Create` (the documented pattern for unit-testing `IEndpointFilter` without a live host) rather than a full `WebApplicationFactory` run, since no endpoint exists yet to attach it to — the literal "does not invoke `dotnet.exe`" claim from spec.md gets additional coverage once §5 wires the real endpoint
- [ ] 3.7 **§9/§10 done, one gap remains for all three**: `BearerAuthenticationFilter` is attached to `POST /run`, `POST /run/git`, and `POST /artifacts` alike (same `.AddEndpointFilter<BearerAuthenticationFilter>()` call each time) — still pending, for all three equally (a gap that predates this section, not specific to git/artifact-transfer): an explicit BDD scenario confirming a real unauthenticated request is rejected through the real mapped endpoint. `ServiceAuthentication.feature` only exercises the filter in isolation via `EndpointFilterInvocationContext` so far. Repository management (§13) deliberately gets **no** REST endpoint at all, so it needs no corresponding bearer-auth coverage here — its auth is the dashboard cookie, covered under §12.

## 4. Path Confinement

- [x] 4.1 Implemented in `Infrastructure.Execution.WorkingDirectoryResolver`: joins the caller-supplied relative path to `ServiceOptions.RootDirectory` via `Path.Combine` + `Path.GetFullPath` (an absolute caller-supplied path correctly discards the root per `Path.Combine`'s own semantics, so it's rejected by containment rather than needing a special case)
- [x] 4.2 `WorkingDirectoryResolver.ResolveSymlinkTarget` calls `Directory.ResolveLinkTarget(path, returnFinalTarget: true)` before the containment check; a real symlink-escape test confirms it (skips gracefully if the sandbox lacks the privilege to create symlinks, per Windows' Developer Mode/elevation requirement)
- [x] 4.3 Containment decision implemented in `Domain.PathConfinement.PathConfinementPolicy.IsContained` (full-path comparison with a trailing separator, not naive `StartsWith`) — pure, zero-IO, called by the Infrastructure resolver above
- [x] 4.4 Unit tests: `PathConfinementPolicyTests` (7 cases: root itself, subdirectory, sibling-prefix rejection, unrelated path, parent-of-root, case-insensitivity, trailing-separator handling) + `WorkingDirectoryResolverTests` (5 cases against a real temp directory tree: valid subdirectory, `../..` traversal, absolute-path escape, sibling-prefix escape, symlink escape)
- [x] 4.5 **§10 done**: `IWorkingDirectoryResolver.ResolveWithinRoot(root, relativePath)` generalizes the confinement/symlink-resolution logic `Resolve` already had, reused for the second, narrower confinement check (file path confined to a specific resolved repo directory, not just the service-wide root) — no new Domain logic, `Resolve` itself is now just `ResolveWithinRoot(configuredRoot, ...)`; unit tests cover the composition (root→repo→file) in `WorkingDirectoryResolverTests`, not duplicating `PathConfinementPolicyTests`. Still pending: §13 (Repository Management) reusing the same primitive for clone/delete target-name resolution.

## 5. Command Execution

- [x] 5.1 Contract implemented: `StartRunRequest` (Presentation) — `Arguments`/`WorkingDirectory`/`TimeoutSeconds` in; `X-Run-Id` response header; `SseWriter` (Presentation) emits `stdout`/`stderr` data events, a terminal `done` event (`{exitCode, truncated}`), and a terminal `error` event (`{reason}`, values `validation`/`timeout`/`cancelled`)
- [x] 5.2 `DotnetProcessRunner.BuildStartInfo` (Infrastructure): `UseShellExecute = false`, arguments added individually to `ArgumentList` — never string-concatenated
- [x] 5.3 `OutputDataReceived`/`ErrorDataReceived` write into an unbounded `Channel`; a single consumer task drains it into `IProcessOutputSink` in order, avoiding concurrent writes to the HTTP response from the two callback threads
- [x] 5.4 `RunEndpoints.HandleStartRunAsync` sets `Content-Type: text/event-stream`, disables response buffering via `IHttpResponseBodyFeature`, and `SseWriter` flushes after every event
- [x] 5.5 `SseProcessOutputSink` tracks cumulative UTF-8 byte count against `ServiceOptions.OutputCapBytes`; once exceeded, further lines are dropped (`Truncated = true`) but the process keeps running
- [x] 5.6 `Domain.Runs.ExecutionTimeoutPolicy.Resolve` computes the effective timeout (caller value or default, clamped to configured maximum); the endpoint links a timeout `CancellationTokenSource` with `HttpContext.RequestAborted`, and `DotnetProcessRunner` kills the process tree (`entireProcessTree: true`) on cancellation
- [x] 5.7 Validation failures (empty/missing arguments, missing working directory, path-confinement rejection) write `error`/`validation` before any process is started — the "repo already locked" case is added when Section 6's registry wraps this endpoint
- [x] 5.8 BDD `DotnetCommandExecution.feature`: successful `dotnet test` round trip against `PassingFixture` — run id present, output events precede `done`, exit code 0
- [x] 5.9 BDD: `dotnet test` against `FailingFixture` returns a non-zero exit code via `done`, not a transport-level error
- [x] 5.10 Unit tests on `DotnetProcessRunner.BuildStartInfo` (no live process needed): `UseShellExecute` is false, and an argument containing shell metacharacters (`; rm -rf / & echo INJECTED > pwned.txt`) round-trips as exactly one literal `ArgumentList` entry
- [x] 5.11 BDD: a non-streaming client (`HttpCompletionOption.ResponseContentRead`, fully buffered) parses the identical set of events as the streaming client
- [x] 5.12 Unit tests on `ExecutionTimeoutPolicy`: caller value shorter than default honored; caller value longer than maximum clamped; omitted caller value uses default; a misconfigured default above maximum is also clamped
- [x] 5.13 BDD: both "no caller timeout, short configured default" and "short caller-supplied timeout" scenarios against `HangingFixture` end in `error`/`timeout` — confirmed no orphaned `testhost.exe`/`dotnet.exe` processes survive the kill

All 10 BehaviorTests scenarios pass in ~14s (real `dotnet.exe` spawns); 27/27 UnitTests; 5/5 ArchitectureTests.

## 6. Concurrency & Locking

- [x] 6.1 Implemented `Application.Concurrency.RunRegistry`: `ConcurrentDictionary<string, Guid>` keyed by the resolved repo root (case-insensitive), mapping to the holding run's id — **simplified from the original `RunInfo` sketch**: the `Process` handle and `CancellationTokenSource` stay local to `RunEndpoints.HandleStartRunAsync`/`DotnetProcessRunner` (they're not needed anywhere else yet); the registry's only job is the lock. Section 8 (Cancellation) will extend it with whatever run-id → cancellation-handle lookup cancelling actually needs
- [x] 6.2 `RunEndpoints` calls `RunRegistry.TryAcquire` (atomic `ConcurrentDictionary.TryAdd`) right after path resolution and before any process is started; on failure, writes an `error` SSE event with reason `validation` and a `runId` field naming the conflicting run — extended `SseWriter.WriteErrorAsync` to carry that optional field
- [x] 6.3 `RunRegistry.Release` called in a `finally` block wrapping process execution, so the lock is freed on every terminal path (completed, timed out) without duplicating release logic per branch
- [x] 6.4 Unit test: 50 concurrent callers racing `TryAcquire` for the same repo — exactly one succeeds (`Barrier`-synchronized to maximize actual contention, not just sequential calls)
- [x] 6.5 BDD `ConcurrencyAndLocking.feature`: `PassingFixture` and `FailingFixture` runs started concurrently (not sequentially awaited) both complete independently
- [x] 6.6 BDD: a second request against an in-flight `HangingFixture` run is rejected, `error`/`validation` carrying the in-flight run's exact id (captured via the first request's `X-Run-Id` header, available immediately since `RunEndpoints` now calls `response.StartAsync()` right after setting headers rather than waiting for the first event write)
- [x] 6.7 BDD: once the in-flight run reaches a terminal state (its own short timeout), a same-repo follow-up request is accepted (no busy-repo rejection) — note it still may itself time out against `HangingFixture`, which is a different, legitimate outcome the test distinguishes from a conflict rejection

All 13 BehaviorTests scenarios pass in ~14s; 32/32 UnitTests; 5/5 ArchitectureTests.

## 7. Documentation, Copyright, and Project Conventions

- [x] 7.1 Created `README.md`: what this project is, links to `BUILD.md`/`INSTALL.md`/`CHANGELOG.md`/`CLAUDE.md`, architecture summary, pointer to `openspec/changes/sbx-dotnet-command-service/` for the full design rationale
- [x] 7.2 Created `BUILD.md`: prerequisites, `dotnet build`/`dotnet test` on `OutOfTheBox.slnx`, the fast (`UnitTests`/`ArchitectureTests`) vs. slow (`BehaviorTests`) split and why, plus a note on the `Fixtures`-not-in-`.slnx` deviation and the doc-comment build gate
- [x] 7.3 Created `INSTALL.md`: today's dev-run instructions (`dotnet run --project src/Host/OutOfTheBox.Host`, required config keys), with a clearly-marked "Planned" section for §17's `install.ps1`/`upgrade.ps1` flow, to be rewritten with concrete instructions once that section lands
- [x] 7.4 Created `CHANGELOG.md` (Keep a Changelog format) with an `Unreleased` section covering Sections 1–6 (Added) and a pointer to `tasks.md` for what's still in progress
- [x] 7.5 Added the copyright header (`// Copyright (c) 2026 Dennis Freise <dennis.freise@final-frontier.org>. All rights reserved.`, from `git config user.name`/`user.email`) to all 33 existing `.cs` files under `src/` and `tests/{UnitTests,BehaviorTests,ArchitectureTests}` via a script (checked each file's first line first, so it's safe to re-run) — excluded `tests/Fixtures/` and the Reqnroll-generated `*.feature.cs` files (regenerated every build, already gitignored, a header would just vanish)
- [x] 7.6 Convention adopted for all subsequent sections: every new `.cs` file starts with the same header
- [x] 7.7 Initialized `CLAUDE.md` via the `init` skill: commands, the Clean Architecture placement quick-reference with the "why `Presentation` can't depend on `Infrastructure`, not even for DI" rationale, and the conventions list (copyright header, doc-comment gate, `Fixtures` exclusion, commit-per-coherent-step, feature-files-land-with-their-capability)
- [x] 7.8 Confirmed `dotnet build` and the full `dotnet test` (all three projects) still succeed after header insertion: 0 warnings/errors, 32/32 UnitTests, 5/5 ArchitectureTests, 13/13 BehaviorTests — also caught and fixed one now-stale in-code comment (`Program.cs` said "Section 12 (Transport & Network)"; renumbered to §13 along with this section's insertion)

## 8. Cancellation API

- [x] 8.1 Already satisfied by §5/§6: `RunEndpoints` assigns `runId` and calls `response.StartAsync()` right after setting the `X-Run-Id` header, before any validation or the SSE body begins
- [x] 8.2 Added authenticated `POST /run/{runId:guid}/cancel`, mapped alongside `POST /run` in `MapCommandExecutionEndpoints`
- [x] 8.3 `RunRegistry` extended with a run-id-keyed index (`ConcurrentDictionary<Guid, RunHandle>`, alongside the existing repo-root index) storing a dedicated `cancelRequestCts` per run; `TryCancel(runId)` calls `.Cancel()` on it, which (linked into the same token passed to `IProcessRunner.RunAsync`) triggers `Process.Kill(entireProcessTree: true)` via the same cancellation-registration mechanism the timeout path already used
- [x] 8.4 The `cancelRequestCts` is a **separate** linked-token source from `timeoutCts` (both linked into one `linkedCts` alongside `RequestAborted`), so `RunEndpoints` can tell which one fired and emit `error`/`cancelled` vs `error`/`timeout` correctly instead of collapsing both into one reason; the registry entry is removed in the same `finally` block as before
- [x] 8.5 Cancel for an unknown/already-terminal run id returns `404 Not Found`; `RunRegistry.TryCancel` also catches `ObjectDisposedException` (a benign race if the run's `CancellationTokenSource` gets disposed between lookup and `.Cancel()`) and treats it identically to not-found — no side effects either way
- [x] 8.6 BDD `Cancellation.feature`: cancelling an in-flight `HangingFixture` run returns 202, its SSE stream ends with `error`/`cancelled`, and a subsequent request against the same repo is accepted (not rejected as busy)
- [x] 8.7 BDD: cancelling an already-completed run (`PassingFixture`, awaited to completion first) and cancelling an unknown run id both return 404; cancelling the same run twice returns 404 the second time **once the first cancellation has actually completed** (drained via the "stream ends with reason" step) — an earlier version of this scenario fired the second cancel immediately and incorrectly expected 404 while the run was still legitimately in-flight (cancellation requested but not yet terminated); fixed by sequencing the test correctly rather than changing the implementation, since spec.md only requires 404 for unknown-or-already-terminal runs
- [x] 8.8 Confirmed `RunRegistry.TryCancel`/the registry's run-id index work unchanged for both `git` runs (BDD: "Cancelling an in-flight git run kills it and frees the repo", §9.10) and artifact transfers (BDD: "Cancelling an in-flight transfer stops it", §10.14; unit: `RegisterTransfer_makes_the_run_cancellable_without_acquiring_any_repo_lock`, §10.7) — kind-agnostic in both cases, as expected, no code changes to `TryCancel` itself were needed for either

All 17 BehaviorTests scenarios pass in ~14s; 36/36 UnitTests; 5/5 ArchitectureTests (58/58 total). No orphaned `testhost.exe` after cancel-triggered kills.

## 9. Git Command Execution

- [x] 9.1 Added `Executable` to `ProcessRunRequest` (`"dotnet"` or `"git"`); renamed `DotnetProcessRunner` → `CliProcessRunner` (`git mv`), using `request.Executable` (never caller-supplied) as `ProcessStartInfo.FileName` — updated all call sites and the renamed `CliProcessRunnerTests`
- [x] 9.2 Added `POST /run/git`, `HandleStartRunAsync` generalized to take an `executable` parameter fixed per endpoint (`"dotnet"` for `POST /run`, `"git"` for `POST /run/git`) via a thin mapping lambda; reuses `StartRunRequest`, `SseWriter`, `RunRegistry.TryAcquire`/`Release`, the timeout/cancellation linked-token plumbing, and `BearerAuthenticationFilter` unchanged
- [x] 9.3 `RunRegistry` needed no code change (already keyed purely by repo root, not run kind) — added `TryAcquire_is_shared_bidirectionally_between_dotnet_and_git_runs_against_the_same_repo` unit test proving both directions explicitly, not just implied by the existing kind-agnostic tests
- [x] 9.4 `POST /run/{runId:guid}/cancel` needed no code change (same run-id-keyed index from §8) — confirmed via the new git cancellation BDD scenario (9.10) actually exercising it end-to-end, not just by inspection
- [x] 9.5 No new validation logic added; confirmed via BDD (`GitCommandExecution.feature`'s "A destructive command is accepted without restriction" scenario posting `["reset", "--hard", "HEAD"]`) rather than a unit test — a real end-to-end round trip through the endpoint is a stronger proof that arguments are never filtered than a unit test on `BuildStartInfo` alone would be, and `CliProcessRunnerTests`' existing shell-metacharacter test (9.11) already covers the unit-level argument-passthrough claim
- [x] 9.6 BDD `GitCommandExecution.feature`: successful `git status` round trip against `GitFixture` (see 9.7 for why `status`, not literally `pull`) — run id present, exit code 0
- [x] 9.7 **Deviation from the original plan**: `GitFixture` is not checked-in static content. `tests/OutOfTheBox.BehaviorTests/Support/GitFixture.cs` generates a real git repo in a fresh temp directory per test run instead (`git init`, a local commit identity, one commit) — a genuine `.git` directory can't cleanly nest inside this repo's own git history (git treats a nested `.git` as a submodule gitlink, not literal trackable content), and git commands genuinely mutate a working tree, so a single checked-in fixture would stop being deterministic across runs the same way the read-only `dotnet` fixtures are. `withBlockingHook: true` additionally installs a `pre-commit` hook (`ping -t 127.0.0.1`, mirroring `HangingFixture`'s `Task.Delay(Timeout.Infinite)` — git has no built-in command that simply never returns) for scenarios needing a genuinely long-running, cancellable `git` command
- [x] 9.8 BDD: `git checkout does-not-exist` delivers its non-zero exit code via `done`, not a transport-level error
- [x] 9.9 BDD, **both directions**, added to `ConcurrencyAndLocking.feature`: (a) "A git run is rejected while a dotnet run is in flight for the same repo" (`HangingFixture` + a `git status` second request); (b) "A dotnet run is rejected while a git run is in flight for the same repo" (`GitFixture` with the blocking hook + a `dotnet --version` second request) — both pass; `specs/dotnet-command-execution` and `specs/git-command-execution` both assert both directions in their own spec text, matching
- [x] 9.10 BDD `Cancellation.feature`: "Cancelling an in-flight git run kills it and frees the repo" — cancel returns 202 and a subsequent run against the same repo is accepted once the process tree is actually torn down; deliberately does **not** assert on the original request's own SSE stream content (see the investigation note below for why that response can't be read reliably in this harness) — the repo becoming free again is sufficient proof the process was killed and the run reached a terminal state
- [x] 9.11 Unit tests on the renamed `CliProcessRunner.BuildStartInfo`: `UseShellExecute` is false and arguments round-trip literally, parameterized over `Executable = "dotnet"` and `"git"` via `[Theory]`/`[InlineData]`; a dedicated `BuildStartInfo_sets_git_as_the_file_name_when_requested` test confirms the executable itself is honored, not hardcoded

**Investigation note (affects 9.9's and 9.10's test implementation, not production behavior):** an `HttpClient` awaiting a `POST /run/git` response (even with `ResponseHeadersRead`) against a request that's genuinely blocked behind a native process tree (`git.exe` → `sh.exe` → the blocking hook's child) was observed, intermittently and not fully root-caused, to not become observable to the caller until the whole request completes - confirmed via temporary server-side instrumentation that `RunRegistry.TryAcquire` itself still succeeds within single-digit milliseconds of the request arriving server-side, so this is a client/transport-observation quirk (suspected scheduling interaction, not a service-side delay - ruled out thread-pool starvation and TLS-layer explanations specifically). Worked around by never awaiting that specific response for synchronization: the git run is fired fire-and-forget, and a **separate** `HttpClient` (reusing the same blocked client for a second concurrent request was itself observed to queue behind it) polls with cheap, disposable probe requests until the lock is observed held (BDD 9.9) or the conflict payload's `runId` is captured (BDD 9.10's cancellation target). None of this affects real deployment (a real sbx caller normally isn't racing its own request against a second one on the same `HttpClient` instance) - it's specific to how this test harness synchronizes two requests against one in-process `WebApplicationFactory`.

All 23 BehaviorTests scenarios pass in ~14s (stable across repeated runs); 40/40 UnitTests; 5/5 ArchitectureTests.

## 10. Artifact Transfer

- [x] 10.1 Added `ArtifactTransferRequest` (Presentation): `Repo`, `Path` (both nullable strings, mirroring `StartRunRequest`'s shape)
- [x] 10.2 Implemented `POST /artifacts` (`ArtifactTransferEndpoints.cs`): resolves `Repo` via the existing `IWorkingDirectoryResolver.Resolve` (unchanged); resolves `Path` against that *resolved* repo directory via a new `IWorkingDirectoryResolver.ResolveWithinRoot(root, relativePath)` method — `Resolve` itself now just calls `ResolveWithinRoot(configuredRoot, ...)`, so the confinement/symlink-resolution logic has exactly one implementation, reused at both levels; `X-Run-Id` is set on the response before any validation, the same "caller has the id regardless of outcome" pattern `POST /run` uses — **deviation from the literal task wording**: no explicit `response.StartAsync()` early-flush is needed here (unlike `POST /run`), since determining success/404/400 is a fast synchronous-ish filesystem check with no long-running process in between: calling `StartAsync()` early would have locked in a 200 status before that check ran, which is wrong for this endpoint (it needs to return real 400/404 status codes depending on outcome, not encode the outcome inside an always-200 body the way the SSE endpoints do)
- [x] 10.3 Confinement violation (either level) or a missing `Repo`/`Path` → `400 Bad Request`, no file opened
- [x] 10.4 Resolved-and-confined path that doesn't exist (or is a directory, not a file — see 10.11's note) → `404 Not Found`, distinct from the `400` above
- [x] 10.5 Success streams the file via `FileStream.CopyToAsync(response.Body, ...)` (not `Results.File`, which doesn't accept an external cancellation token — see 10.8) with `Content-Type: application/octet-stream` and an explicit `Content-Length`; never buffers the whole file
- [x] 10.6 Confirmed `POST /artifacts` never calls `RunRegistry.TryAcquire` — BDD "A transfer proceeds while a command is in flight against the same repo" (`HangingFixture`) proves it directly
- [x] 10.7 Added `RunRegistry.RegisterTransfer`/`ReleaseTransfer`, touching only the run-id-keyed index, never the repo-root-keyed one `TryAcquire`/`Release` use — unit tests prove a registered transfer doesn't block a `TryAcquire` for the same repo, and that `ReleaseTransfer` makes the run id uncancellable again
- [x] 10.8 `POST /run/{runId:guid}/cancel` needed no code change (same run-id-keyed index, kind-agnostic) — the file copy observes a `CancellationTokenSource` linked into `RequestAborted`, registered via `RegisterTransfer`, so cancelling it stops `CopyToAsync` mid-copy; BDD confirms the client receives fewer bytes than the declared `Content-Length`
- [x] 10.9 BDD `ArtifactTransfer.feature` "Successful transfer of a file a build produced": runs a real `dotnet test --results-directory TestResults --logger "trx;LogFileName=results.trx"` against `PassingFixture` first (via `POST /run`) to produce a genuine build-produced file, then transfers it and confirms the bytes match exactly — **deviation**: not the compiled DLL itself, because the centralized `artifacts/` output layout (§20.9) now builds every project's `bin`/`obj` *outside* its own directory entirely, so a compiled binary is no longer reachable through a repo-confined transfer at all; a test-results file written via an explicit `--results-directory` flag (independent of the standard build-output pipeline) is still a faithful "file a build process produced," without fighting the centralized-output decision
- [x] 10.10 BDD: a path-traversal attempt and an absolute-path attempt are both rejected `400`, no file read
- [x] 10.11 **Deviation**: implemented as a unit test (`ResolveWithinRoot_composed_twice_follows_a_symlink_that_escapes_the_named_repo`), not BDD — matching the precedent already set by `Resolve`'s own symlink-escape case (§4.4), which was also unit-level only, never exercised through a full BDD/HTTP scenario in this codebase; the composition reuses the exact same `ResolveSymlinkTarget`/`PathConfinementPolicy` machinery `Resolve` already has unit coverage for, just at the second confinement level. Also added: a resolved-and-confined path pointing at a *directory* is rejected (`404`, same as a missing file) rather than transferred or listed, per the "No directory listing" requirement
- [x] 10.12 BDD: a well-formed request for a nonexistent file returns `404`, distinguishable from the `400` confinement-violation scenarios
- [x] 10.13 BDD: a transfer started while a `dotnet test` is in flight against the same repo succeeds (not rejected as busy)
- [x] 10.14 BDD: cancelling an in-flight transfer of a 100 MB fixture file (generated at test time, deleted afterward — not checked in) stops the copy; the client observes fewer bytes than the file's declared `Content-Length` (or an `IOException`/`HttpRequestException` from the truncated response, caught and treated the same way) — stable across repeated runs
- [x] 10.15 Unit tests: the two-level confinement composition (root→repo, then repo→file) rejects a sibling-repo path, `..`-traversal within the named repo, and an absolute path, and accepts a genuinely nested valid path — added to the existing `WorkingDirectoryResolverTests` fixture (one extra sibling "other-repo" directory + a nested file), not a new test class

All 29 BehaviorTests scenarios pass in ~14s (stable across repeated runs, including the cancellation scenario); 47/47 UnitTests; 5/5 ArchitectureTests.

All BehaviorTests/UnitTests/ArchitectureTests counts updated at the end of this section once implemented.

## 11. Persistence (Run History + Resource Samples)

- [x] 11.1 Add EF Core + SQLite provider packages; define `Run` entity (`Id`, `Kind` (`DotnetCommand`/`GitCommand`/`ArtifactTransfer`/`RepositoryClone`/`RepositoryDelete`), `RepoPath`, `Arguments` (nullable), `ArtifactPath` (nullable), `ArtifactSizeBytes` (nullable), `SourceUrl` (nullable — `RepositoryClone` only), `StartedAt`, `CompletedAt`, `Outcome` (including the new `NotFound` and `AlreadyExists` values), `ExitCode`, `Stdout`, `Stderr`, `Truncated`) and initial migration
- [x] 11.2 Apply pending migrations against the configured SQLite file path at startup
- [x] 11.3 Enable WAL journal mode on the SQLite connection
- [x] 11.4 Insert a `Run` row with `Outcome = Running` and the correct `Kind` at the same point each kind's in-memory registry entry is created (repo lock acquired for `dotnet`/`git`/clone; transfer or delete registered) — **deviation**: `RepositoryClone`/`RepositoryDelete` aren't implemented yet (§13), so only `dotnet`/`git`/transfer insert a row today; the insert call sites for clone/delete land with §13 itself, reusing the same `IRunRepository.AddAsync`
- [x] 11.5 Update the `Run` row's terminal fields at the same point the registry entry is removed, branching by kind: `CompletedAt`/`Outcome`/`ExitCode`/`Stdout`/`Stderr`/`Truncated` for `dotnet`/`git`/clone; `CompletedAt`/`Outcome`/`ArtifactSizeBytes` for a transfer; `CompletedAt`/`Outcome` only for a delete — one code path per kind, not one path that awkwardly handles every shape (clone/delete deferred to §13, same as 11.4)
- [x] 11.6 On startup, reconcile any row still `Outcome = Running` from a previous process to `Outcome = Interrupted`, regardless of kind
- [x] 11.7 Add `IRunRepository.ListAsync(RunQuery query)` where `RunQuery` bundles optional `Kinds`, `Outcomes`, `Repo`, and `SearchText`, composed as a single conditionally-filtered EF Core `IQueryable`; add a query for one run's full record by id — **deviation**: the free-text search clause and the final `StartedAt` ordering are applied client-side after materializing the server-side-filtered (`Kinds`/`Outcomes`/`Repo`) set, not as part of the same `IQueryable` — the SQLite provider can't translate `ORDER BY` over a `DateTimeOffset` column, and routing the `Arguments` search term through `EF.Property<string>` to reach its JSON-converted column caused EF Core to misapply that column's value converter to the search parameter itself at execution time (an EF Core bug, not a modeling error); both were caught by actually running the query against real SQLite in a unit test, not by design review, and are safe at the row counts this table is expected to reach (see design.md's already-accepted "no retention policy" trade-off)
- [x] 11.8 Unit test (SQLite `:memory:`): a `dotnet`/`git`/clone run's row exists with `Outcome = Running` while in flight, then is updated to its terminal outcome with full output on completion — **deviation**: covers `dotnet`/`git` only; `clone` has no implementation to test yet (see 11.4)
- [x] 11.9 Unit test: a transfer's row exists with `Outcome = Running` while in flight, then is updated with `ArtifactSizeBytes` and a completed outcome
- [x] 11.10 Unit test: persisted stdout/stderr and truncation flag match what was streamed for a truncated run
- [x] 11.11 BDD: a run's history record (of each kind) is retrievable after a service restart — **deviation**: covers `dotnet`/`git`/transfer (`clone`/`delete` don't exist yet, see 11.4); asserts by resolving `IRunRepository` directly from a second `WebApplicationFactory` pointed at the same SQLite file, not over HTTP — there's no run-history query endpoint yet (Section 12's dashboard is the only planned consumer, and it calls the repository in-process, never over REST)
- [x] 11.12 Unit test: a row left `Running` by a simulated crash is reconciled to `Interrupted` on next startup, regardless of kind — covers `dotnet`/`git`/transfer; `ReconcileInterruptedAsync` is kind-agnostic (`WHERE Outcome = Running`), so clone/delete need no new reconciliation logic once §13 adds them, only their own 11.8/11.9-style tests
- [x] 11.13 Unit tests for `ListAsync`: filter by a single kind; filter by multiple kinds; filter by outcome; filter by repo alone (no kind/outcome filter); free-text search matching repo, arguments, artifact path, and clone source URL; two filters combined; search combined with a filter; no filters returns everything
- [x] 11.14 Define `RunResourceSample` entity (`RunId` FK, `Timestamp`, `CpuPercent`, `RamBytes`) with an index on `(RunId, Timestamp)`, and its migration — used by every run kind that gets one (not deletes — see §13)
- [x] 11.15 Add a query returning a run's complete resource-sample series ordered by `Timestamp`, for both in-flight and completed runs of any kind — same client-side-ordering deviation as 11.7 (SQLite/`DateTimeOffset`); covered by a unit test against a real SQLite `:memory:` connection
- [ ] 11.16 BDD: a completed run's full resource-sample series (start to terminal state) is retrievable regardless of run duration, for a command run and for a transfer — **blocked**: nothing writes a `RunResourceSample` row yet: `IResourceSampler` and the background sampler tick that would populate this table are Section 14 (Host Resource Monitoring), not yet implemented. `RunResourceSample`'s entity/migration/query (11.14/11.15) are done and unit-tested at the repository level; this specific end-to-end BDD scenario needs a real sampler producing samples during a live run and is deferred to land alongside Section 14/15

All 65 UnitTests, 5 ArchitectureTests, and (with 11.16 deferred) 32 BehaviorTests pass.

## 12. Dashboard (Blazor Server)

- [x] 12.1 Add Blazor Server to the existing minimal API project (`AddServerSideBlazor`, root component host page) — **deviation**: implemented via .NET 8+'s unified `AddRazorComponents().AddInteractiveServerComponents()` / `MapRazorComponents<App>().AddInteractiveServerRenderMode()` API instead of the literal (now-legacy) `AddServerSideBlazor()`/`MapBlazorHub()`/`_Host.cshtml`/Razor Pages combo the task names — same end behavior (interactive server-rendered Blazor over a SignalR circuit), but the unified model needs no separate Razor Pages root host page and is the officially recommended approach for .NET 10; judged a directional match for "add Blazor Server hosting," not a literal API requirement
- [x] 12.2 Implement `IRunEventBus` singleton (run-started / output-line (`dotnet`/`git`/clone only) / run-terminal events, all carrying `Kind`) published to from the same execution code paths as the registry and the SQLite writes, for every run kind — **deviation**: `RepositoryClone`/`RepositoryDelete` publish call sites land with §13 (those kinds don't exist yet, same scoping already noted for §11); output-line events are published but nothing subscribes to render them yet (Status view's scope per 12.7 is summary fields only, not a live output preview) - wiring is in place for a future consumer without speculative UI for it now
- [x] 12.3 Build login page: operator enters the shared token, validated via the same `CryptographicOperations.FixedTimeEquals` check (reused via the existing `CredentialComparer` domain helper), issues an auth cookie on success — implemented as a plain minimal-API `POST /account/login` handler (not a Blazor component method): a Blazor Server circuit can't set a response cookie mid-circuit, so the login page's `<form>` posts here as a genuine full-page HTTP request. **Bug caught only by actually running the login flow** (not by the test suite or review): `/account/login`/`/account/logout` were originally mapped at `/login`/`/logout` directly, colliding with the Blazor page route `/login` and throwing `AmbiguousMatchException` at request time (`MapRazorComponents` registers a page route against every HTTP method, to support Blazor's own enhanced-navigation form posts) - moved under `/account/...`. **Second bug, same root cause**: a handler shaped `(HttpContext) => Task<IResult>` binds to minimal API's older `RequestDelegate`-shaped overload, which discards the `IResult` and does nothing (200 with no `Location` header instead of the intended redirect) - reproduced for the single-`HttpContext`-parameter logout handler specifically, not the two-parameter login handler, matching an ASP0016 build warning that turned out not to be a false positive; fixed by using `HttpContext.Response.Redirect(...)` directly instead of `Results.Redirect(...)`
- [x] 12.4 Require the auth cookie on all dashboard routes/circuits; unauthenticated access shows no run data — `MapRazorComponents<App>()...RequireAuthorization()` on the whole Razor Components route group (not a global `FallbackPolicy`, which would have also swept up the bearer-token-protected API endpoints and required a cookie there too); `Login.razor` opts back out via `[AllowAnonymous]`. Verified against a real running instance (not just `WebApplicationFactory`): `GET /` without a cookie redirects to `/login`; `POST /account/login` with the correct token sets the cookie and grants access; `POST /account/logout` clears it
- [x] 12.5 Add a single dark CSS theme; no light-mode stylesheet, no `prefers-color-scheme` branching, no toggle
- [x] 12.6 Build the shared dashboard layout/shell: persistent header showing "OutOfTheBox v{version}" (reading the `Directory.Build.props`-sourced version via `AssemblyInformationalVersionAttribute` reflection, per design.md — factored into a shared `VersionInfo.Current` so `/version` and the layout read the exact same value, not two separate reflection calls), present on every route; top-level navigation with three views: **Status** (default), **History**, and **Repos** (§13 builds the Repos view itself; this task added only the nav entry and a placeholder page so the link isn't a dead 404)
- [x] 12.7 Build Status view's run list: in-flight runs of every kind (repo, kind badge, run id, start time, elapsed time, arguments for command runs / requested path for transfers / source URL for clones), subscribed to `IRunEventBus` for live updates via `StateHasChanged` — **deviation**: sourced from `IRunRepository.ListAsync(new RunQuery { Outcomes = [Running] })` (the SQLite `Runs` table), not literally `RunRegistry` as worded — `RunRegistry`'s entries carry only a run id and cancellation token source (no `Kind`/`RepoPath`/`Arguments`/timestamps), and extending its shape to carry display metadata would touch every existing acquire/release call site across Sections 9-11 for data that's already available, exactly synchronized (same insert/terminal-update points), in the `Runs` table. Also flagged: `IRunRepository`/`IRunResourceSampleRepository` are injected directly into Blazor components even though they're `Scoped`/EF-Core-backed - Blazor Server ties a `Scoped` service's lifetime to the circuit (not the request), so a long-open browser tab reuses one `DbContext` instance across its whole session; accepted since every query in `EfRunRepository` is `AsNoTracking` (no change-tracker state accumulates) and this is a single/few-operator internal tool, not `IDbContextFactory`-per-operation correctness-critical territory
- [x] 12.8 Build "idle" empty-state for the Status view when no runs are in flight
- [x] 12.9 Build History view's list: queries `Runs` table most-recent-first with summary fields, each row showing its kind badge, linking to a routable run-detail subpage (`/history/{runId}`) rather than expanding inline
- [x] 12.10 Build History view's filter/search controls: kind multi-select, outcome filter, **repository filter** (text match against a repo path — the explicit "filter by repo" requirement), debounced (300ms) free-text search box — all bound to one `RunQuery` component state object passed to `IRunRepository.ListAsync`, no separate client-side-only filtering logic
- [x] 12.11 Build the run-detail subpage (`/history/{runId}`), branching by kind: `dotnet`/`git`/clone runs show full command (or source URL for clone), repo, timestamps, outcome, complete stdout/stderr, truncation indicator; artifact transfers show repo, requested file path, file size, timestamps, outcome — **deviation**: delete's branch (repo name, timestamps, outcome only) lands with §13, since `RepositoryDelete` runs don't exist yet
- [x] 12.12 BDD: a new run of each kind appears in the Status view live, without reload, when started by another client (`service-dashboard.feature`) — **deviation, gap since closed**: the tooling gap noted when §12 was first implemented (no Blazor-interactive test client) is closed by adding `bUnit` (component-level rendering, not a real browser/SignalR transport) - `StatusComponentTests.A_new_run_appears_live_when_a_Started_event_is_published_without_reload` renders the real `Status` component, publishes a `RunEvent` on a real `IRunEventBus`, and asserts (via `WaitForAssertion`, which polls rather than assuming synchronous rendering) that the run appears without ever calling any render method directly - genuinely exercises `IRunEventBus.Subscribe` → `InvokeAsync` → `StateHasChanged`, not just code review
- [x] 12.13 BDD: a run's completion is reflected live (status updates / moves to history) without reload, for each kind — closed the same way as 12.12 (`StatusComponentTests.A_completed_run_disappears_live_when_a_Terminal_event_is_published_without_reload`); "for each kind" is satisfied structurally (the component's re-query is kind-agnostic - it re-runs the same `Outcomes = [Running]` query regardless of which kind's event fired) rather than one duplicate test per kind
- [x] 12.14 BDD: History view and run-detail subpage render correctly for completed, timed-out, cancelled, not-found (transfer/delete), already-exists (clone), and interrupted runs, across every kind — `RunDetailComponentTests` (bUnit) covers all seven kind/outcome combinations named here plus a plain completed run, rendering the real `RunDetail` component against a real SQLite-backed `IRunRepository`
- [x] 12.15 BDD: filtering history by kind shows only matching runs; filtering by repository alone shows only that repo's runs across every kind; searching narrows the list; clearing filters/search restores the full list — `HistoryComponentTests` (bUnit) drives the real `History` component's checkboxes/inputs/buttons (via bUnit's `.Change`/`.Input`/`.Click`, not direct method calls) and asserts on rendered markup, including the 300ms search debounce actually elapsing
- [x] 12.16 Add a `/version` endpoint exposing the running build's assembly version (the same value the header/footer branding in 12.6 reads), for upgrade verification and for any external check that doesn't want to open the dashboard — deliberately unauthenticated (a version/health check needs to be reachable without the credential first)

Also completed as a prerequisite: task 1.12 (`<Version>1.0.0</Version>` in `Directory.Build.props`), left unchecked since Section 1 - needed for `VersionInfo`/`/version` to report anything meaningful. `dotnet build`/`dotnet test` confirmed unaffected.

65/65 UnitTests, 5/5 ArchitectureTests, 38/38 BehaviorTests pass (32 prior + 6 new `ServiceDashboard.feature` scenarios: unauthenticated redirect, login success/failure, logout, branding visible in prerendered HTML, unauthenticated `/version`). Manually smoke-tested against a real running `Host` instance (not just `WebApplicationFactory`) for the full login/cookie/logout/version HTTP flow, since that's what caught both bugs noted in 12.3 - `dotnet build`/`dotnet test` alone would not have.

## 13. Repository Management

- [x] 13.1 Define `RepositorySummary` (Domain, or an Application DTO): name, total size in bytes, git status (branch, dirty/clean, ahead/behind, or "not a git repo"), active/idle — placed in Domain per design.md's own Architecture list, which names it alongside `Run`/`RunResourceSample`; added a `StatsComputed` flag (not in the original field list) so a repository the background sampler hasn't reached yet can render "computing…" rather than a wrong zero size
- [x] 13.2 Implement `IRepositoryManager.ListAsync()` (Application/Infrastructure): enumerate top-level directories under `ServiceOptions.RootDirectory`, joined with the latest cached stats (13.4) and live active state (13.6) — a repository with no cached stats yet (service just started) shows a clear "computing…" state rather than a wrong/blank one
- [x] 13.3 Implement git-status querying: internal, unstreamed `git.exe` invocations (`rev-parse --abbrev-ref HEAD`, `status --porcelain`, an ahead/behind count against the upstream if configured) reusing `CliProcessRunner`'s (`IProcessRunner`) process-spawning mechanics but not going through `RunEndpoints`/SSE/`RunRegistry`/history — this is telemetry, not an operator-triggered run; a directory that isn't a git repo at all is detected (no `.git` directory) and reported as such without spawning any process at all
- [x] 13.4 Implement the slow background stats sampler (`RepositoryStatsSampler : BackgroundService`): a `PeriodicTimer` (default 60s, `ServiceOptions.RepositoryStatsSamplerIntervalSeconds`) that recomputes size/git status for every repository and caches the result (also computed once immediately at startup, not just on the first tick); also triggered immediately whenever a run against a specific repo (any kind) reaches a terminal state, recomputing just that repo — required adding `RepoPath` to `RunEvent` (Section 12 didn't carry it, since Status/History only ever needed "something changed, re-fetch") so the event-driven recompute knows which repo without a DB round-trip
- [x] 13.5 Confirm "active" needs no polling: derive it directly from whether `RunRegistry` currently holds a given repo's key — added `RunRegistry.IsHeld(repoRoot)`, a read-only check (no side effect, unlike `TryAcquire`), unit-tested directly
- [x] 13.6 Implement `IRepositoryManager.CloneAsync(url, name)`: resolve/confine `name` under the root (reject escape or an already-existing target with outcome `AlreadyExists`, recorded as its own terminal history row since there's no "Running" phase to precede it), `RunRegistry.TryAcquire` keyed on the resolved target path, run `IProcessRunner` with `Executable = "git"`, `Arguments = ["clone", url, resolvedPath]`, publish to `IRunEventBus`/write to `Runs` with `Kind = RepositoryClone` and `SourceUrl = url` exactly as `POST /run/git` would, release the lock in `finally` — **deviation**: the clone itself runs as a fire-and-forget background continuation (`CloneAsync` returns as soon as the lock is acquired and the `Running` row is inserted, not once the clone finishes), per specs/service-dashboard's "the clone starts, appears as an in-flight run" requirement — an operator's dashboard click can't block for a clone's full duration. The background continuation resolves its own `IRunRepository` from a fresh `IServiceScopeFactory`-created scope for its terminal `UpdateAsync`, not the scope that started it, since the Blazor circuit that triggered the clone may not outlive it
- [x] 13.7 Unit/BDD test: a `dotnet`/`git` command targeting a repository name that's still mid-clone is rejected as busy (per `specs/repository-management`'s "Commands against a mid-clone target are rejected" scenario) — confirms `RunRegistry.TryAcquire` works unchanged for a not-yet-existing directory key. Covered by BDD (`RepositoryManagement.feature`); the "clone in flight" state is simulated by pre-acquiring the lock directly rather than racing a real (near-instant, tiny local) clone to completion — deterministic and exercises the exact same conflict path
- [x] 13.8 Implement `IRepositoryManager.DeleteAsync(name)`: resolve/confine `name`, `RunRegistry.TryAcquire` for that repo (reject with a conflict, naming the in-flight run, if already held — **acquire**, don't just check-then-act, to close the TOCTOU race per design.md), `Directory.Delete(path, recursive: true)`, write `Kind = RepositoryDelete` to `Runs` (assigned a run id, inserted `Running`, immediately updated terminal — no real streaming phase, runs synchronously to completion unlike clone), release the lock in `finally`; a nonexistent target returns a not-found outcome (its own terminal history row, `Outcome = NotFound`) rather than silently succeeding
- [x] 13.9 Unit test: `DeleteAsync` against a repository currently holding the lock is rejected with a conflict, and no files are removed — covered at both unit level (`RepositoryManagerTests`) and BDD level (`RepositoryManagement.feature`'s "Deletion of a busy repository is rejected"); the BDD scenario simulates the in-flight command via a pre-acquired `RunRegistry` lock rather than a real `dotnet build`/`git` command for the same determinism reason as 13.7 - a first attempt using a real, genuinely-still-running `git commit` (reusing §9's blocking-pre-commit-hook fixture) worked functionally but made this one scenario's teardown take 100+ seconds instead of milliseconds (the hung request's server-side handler was still blocked in `Process.WaitForExitAsync` when `WebApplicationFactory` tried to dispose); switched to the deterministic simulation instead of chasing that interaction further
- [x] 13.10 Confirmed by code review (not a dedicated test): `CloneAsync`/`DeleteAsync`/`ListAsync` are never referenced by any `IEndpointRouteBuilder` mapping extension method - `IRepositoryManager` has exactly one production call site, `RepositoryManager`'s own registration in `Program.cs`, consumed only by Blazor component code-behind (`Repos.razor`, `RepoDetail.razor`). A dedicated reflection-based "no endpoint references this interface" test was judged more complex than the guarantee it would add over reviewing the (small) set of `Map*Endpoints` methods directly
- [x] 13.10a Confirm `POST /run/{runId:guid}/cancel` returns 404 for a repository-clone run id — **bug found and fixed while implementing this**: the endpoint originally called `RunRegistry.TryCancel` unconditionally (kind-agnostic, as it always has been), so it happily cancelled a clone's run id too. Fixed in `RunEndpoints.HandleCancelRunAsync` by looking up the run's persisted `Kind` first and refusing when it's `RepositoryClone`/`RepositoryDelete`. **Second bug in the fix's first version**: treating "no row found yet" the same as "must be repository-management, refuse" broke real `dotnet`/`git` cancellation - `POST /run`/`POST /run/git` send the `X-Run-Id` header (`response.StartAsync()`) *before* their own `IRunRepository.AddAsync` call completes, so a fast cancel request can legitimately race ahead of the row existing; caught by the existing `Cancellation.feature` suite failing, not by review. Fixed by only refusing when the row is positively found to be `RepositoryClone`/`RepositoryDelete`, falling through to the normal registry check otherwise (matching pre-fix behavior exactly for every other kind)
- [x] 13.11 Build the Repos view (Presentation): list every repository with size, git status, and a live active/idle indicator subscribed to `IRunEventBus` (updates without reload); filter controls (active/idle, git status clean/dirty/no-git) and a free-text name search box
- [x] 13.12 Build the clone control: a form prompting for source URL and name, calling `IRepositoryManager.CloneAsync` directly; on success the new repo appears in the list once the clone's terminal event fires
- [x] 13.13 Build the per-repository delete control: a button that requires an explicit confirmation step (first click arms a "Confirm delete?" state, second click actually deletes) before calling `IRepositoryManager.DeleteAsync` — no accidental single-click deletion
- [x] 13.13a Build a cancel control for an in-flight clone (shown on its Repos row while cloning, in place of the Delete button), calling `RunRegistry.TryCancel` directly (in-process, same as the process-kill button design.md describes) — not `POST /run/{runId}/cancel`
- [x] 13.14 Build the repo-detail subpage (`/repos/{name}`): full stats (size, git status, active/idle) plus that repository's run history, reusing the same `IRunRepository.ListAsync`/`RunQuery` mechanism the History view uses, pre-filtered to this repo's resolved path — **deviation**: does not reuse History's actual Razor list *component*, since History's list was built as a single page, not a separable component; `RepoDetail.razor` renders its own (smaller) run-list markup against the same query mechanism instead. A future refactor could extract a shared `RunListing` component if this duplication becomes a real maintenance cost
- [x] 13.15 BDD `RepositoryManagement.feature`: cloning a new repository via the dashboard action succeeds, records history with `Kind = RepositoryClone` and the source URL, and the repository appears in the Repos list once complete — against a real local git source (a `GitFixture` repo, reused from §9) so `git clone` genuinely runs, not a network-dependent remote URL
- [x] 13.16 BDD: cloning into a name that already exists is rejected without invoking `git.exe` or touching the existing repository
- [x] 13.17 BDD: deleting an idle repository removes it from disk and from the Repos list, and records `Kind = RepositoryDelete` in history
- [x] 13.18 BDD: deleting a repository with an in-flight command is rejected, the repository and its files are untouched, and the in-flight run is still recorded as running — **deviation**: the spec's "in-flight `dotnet build`" is simulated (see 13.9's note) rather than driven by a real process, and the "unaffected" assertion checks the persisted run row's `Outcome` stays `Running` and the registry lock stays held, not a real process still executing
- [x] 13.19 BDD: the Repos view's active indicator updates live when a run starts/ends against a repository, without reload — **gap since closed** (see 12.12): `ReposComponentTests.Active_indicator_updates_live_when_a_run_starts_and_ends_against_a_repository` (bUnit) renders the real `Repos` component against a real `RepositoryManager`/`RunRegistry`, acquires the lock and publishes a `Started` event, asserts (via polling `WaitForAssertion`) the row shows "Active", then releases and publishes `Terminal` and asserts it reverts to idle
- [x] 13.20 BDD: git status shown for a repository updates after a run against it completes, without a manual refresh — `ReposComponentTests.Git_status_refreshes_live_after_a_run_against_the_repository_completes` updates `RepositoryStatsCache` (what `RepositoryStatsSampler`'s event-driven recompute does after a real `git pull`) and publishes a `Terminal` event, asserting the rendered git-status text changes without any explicit re-render call from the test
- [x] 13.21 BDD: filtering the Repos view by active/idle or git status, and searching by name, narrows the visible list correctly; clearing restores the full list — `ReposComponentTests` drives the real search input and "Clear filters" button (bUnit `.Input`/`.Click`) against two seeded repositories and asserts on rendered markup

All 74 UnitTests (7 new `RepositoryManagerTests` + 1 new `GitRepositoryStatsProviderTests` covering the non-git fast path + 1 new `RunRegistry.IsHeld` test), 5/5 ArchitectureTests, and 47/47 BehaviorTests (38 prior + 9 new `RepositoryManagement.feature` scenarios) pass, stable across repeated runs. Manually smoke-tested against a real running `Host` instance (not just `WebApplicationFactory`): logged in, confirmed the Repos view correctly lists a real on-disk directory with its actual computed size and "not a git repo" status (proving the background sampler → cache → `ListAsync` → render pipeline works end to end), and confirmed the Status/History/repo-detail pages all render without error with the new wiring in place.

**Follow-up, on explicit request: closed the Blazor-interactive testing gap 12.12-12.15 and 13.19-13.21 left open.** Added `bunit` (2.9.0) as a `UnitTests`-only package plus a new `ProjectReference` to `Presentation` (test-only - `Presentation` itself still never references `Infrastructure`), and four new test files under `tests/OutOfTheBox.UnitTests/Presentation/Dashboard/`: `StatusComponentTests`, `ReposComponentTests`, `HistoryComponentTests`, `RunDetailComponentTests` (22 tests total). These render the real `.razor` components against real `IRunEventBus`/`RunRegistry`/`RepositoryManager`/SQLite-`:memory:`-backed `IRunRepository` instances - no fakes for the logic under test, only for genuinely out-of-scope dependencies (`IProcessRunner`, `IRepositoryStatsProvider` in the Repos tests, which throw if reached, since those tests seed state directly rather than driving a real clone). bUnit 2.x's API (`BunitContext`/`Render<T>`, not the older `TestContext`/`RenderComponent<T>`) required adapting past what the package's own obsolete-API warnings suggested - this project's earlier `TestContext` naming for `WebApplicationFactory` fixtures is unrelated and unaffected. Live-update assertions use bUnit's `WaitForAssertion` (polls until true or times out) rather than asserting immediately after publishing an event, since the component's own `IRunEventBus` handler dispatches its re-render asynchronously via `InvokeAsync` - this is what makes these tests a genuine test of the async update path rather than a race that happens to pass. 96/96 UnitTests (74 prior + 22 new), 5/5 ArchitectureTests, 47/47 BehaviorTests all pass; 0 build warnings.

## 14. Host Resource Monitoring

- [x] 14.1 Implement host CPU sampling via `PerformanceCounter` (`\Processor(_Total)\% Processor Time` and per-core `\Processor(N)\% Processor Time`), discarding each counter's first (always-zero) reading
- [x] 14.2 Implement host RAM sampling via `GlobalMemoryStatusEx` P/Invoke (total/available physical memory)
- [x] 14.3 Implement service-process RAM via `Process.GetCurrentProcess().WorkingSet64`
- [x] 14.4 Implement process-tree discovery: WMI `Win32_Process` query by `ParentProcessId`, recursively rooted at each tracked `dotnet`/`git`/clone run's `Process.Id` from the run registry (artifact transfers and deletes have no process to root a tree at, and are skipped by this discovery step)
- [x] 14.5 Implement per-process CPU% via delta-sampling `Process.TotalProcessorTime` between ticks, and per-process RAM via `WorkingSet64`
- [x] 14.6 Implement a background `PeriodicTimer`-based sampler (configurable interval, default a few seconds) that samples host + per-run-tree data each tick and publishes a snapshot to a new `IResourceEventBus`; for each in-flight artifact transfer, also emit that tick's host-level CPU/RAM figures tagged with the transfer's run id (per design.md's "host-level sampling for transfers" decision) — this sampler is distinct from and runs at a different cadence than §13's repository-stats sampler
- [x] 14.7 Implement `IProcessMonitor.KillAsync(pid)`: re-verify `pid` is currently part of a tracked run's process tree (including re-checking `Process.StartTime` to guard against PID reuse) immediately before killing; reject if not found; call `Process.Kill(entireProcessTree: true)` on the verified target
- [x] 14.8 Wire the Status view's per-run process sublist to `IResourceEventBus`, grouped under its owning `dotnet`/`git`/clone run (not one global flat table, and never shown for a transfer or delete), with a kill button per process calling `IProcessMonitor.KillAsync` directly (no new HTTP endpoint)
- [x] 14.9 Add host CPU (total + per-core) and RAM (total + service) tiles to the Status view header
- [x] 14.10 Unit test: total and per-core CPU figures and total/service RAM figures update on the configured interval (using a fake clock/timer) — deviation: `HostResourceSamplerTests.SampleAsync_can_be_called_repeatedly_on_the_configured_interval` uses a real short delay instead of a fake clock, since `PerformanceCounter`/WMI read live OS state that a fake clock can't drive; it verifies consecutive ticks keep succeeding with internally-consistent, in-range data. The interval-driven *scheduling* itself (fake-clock-independent, since `PeriodicTimer` isn't under test) is exercised via `HostResourceSamplerServiceTests.TickAsync_writes_one_sample_per_tick_across_repeated_calls` calling `TickAsync` directly twice
- [x] 14.11 BDD: a run's spawned children (e.g. a `testhost.exe` under a `dotnet test` run) appear in that run's process sublist with plausible CPU/RAM values (`host-resource-monitoring.feature`)
- [x] 14.12 BDD: killing a listed process terminates it and its descendants, and it disappears from the list on the next refresh
- [x] 14.13 Unit test: `IProcessMonitor.KillAsync` rejects a PID outside any tracked run's process tree
- [x] 14.14 BDD: process list is empty when no `dotnet`/`git`/clone runs are in flight, with no stale entries from a previous run, and remains empty for an in-flight artifact transfer or delete
- [x] 14.15 Unit test: a run's aggregate CPU%/RAM figure equals the sum of that tick's per-process values across its tree (`dotnet`/`git`/clone runs only) — `ResourceAggregationTests.Sum_adds_cpu_and_ram_across_every_sample` covers the pure aggregation math `HostResourceSampler` calls per run tree
- [x] 14.16 Unit test (SQLite `:memory:`): one `RunResourceSample` row is written per in-flight run per tick (every kind that gets one), stopping once the run reaches a terminal state; for a transfer, the row's CPU/RAM values equal that tick's host-level figures — deviation: "stopping once terminal" isn't a check inside `TickAsync` itself, it falls out structurally from `TickAsync` only ever writing rows for the runs present in that tick's `ResourceSnapshot.Runs`, which for the real `IResourceSampler` is driven by `RunRegistry`'s tracked process roots (removed on release/completion) — already covered by 14.12/14.14's BDD scenarios showing the process list (backed by the same registry) goes empty after kill/completion, so a redundant fake-sampler unit test asserting the same structural fact wasn't added
- [x] 14.17 Maintain an in-memory 10-minute circular buffer per live series (host total/per-core, each in-flight run of any sampled kind) fed by the same tick, independent of the SQLite write
- [x] 14.18 Unit test: the circular buffer evicts points older than 10 minutes while the persisted series (from 14.16) keeps every point — `ResourceHistoryBufferTests.Points_older_than_10_minutes_are_evicted_on_the_next_add` (eviction) contrasted with `HostResourceSamplerServiceTests.TickAsync_writes_one_sample_per_tick_across_repeated_calls` (persisted series keeps every tick's row)

19 new UnitTests (5 `ResourceAggregationTests` + 5 `ResourceHistoryBufferTests` + 2 `ProcessMonitorTests` + 3 `HostResourceSamplerTests` + 4 `HostResourceSamplerServiceTests`) plus 4 new `HostResourceMonitoring.feature` BehaviorTests scenarios, plus 3 new bUnit `StatusComponentTests` closing the same live-update gap this section's own `IResourceEventBus` wiring opened (host tiles update live, process sublist appears/groups correctly, kill button calls `IProcessMonitor` with the right PID/start-time). 118/118 UnitTests, 5/5 ArchitectureTests, 51/51 BehaviorTests (47 prior + 4 new) all pass, stable across repeated runs; 0 build warnings. Two real bugs caught and fixed before/while testing, not by review alone: (1) host RAM figures (history buffer + transfer host-tagging) originally used `TotalRamBytes` (a constant) instead of `TotalRamBytes - AvailableRamBytes` (used); (2) `Status.razor`/`Repos.razor`'s percent/byte formatting used current-culture `:F1` interpolation, which renders comma-decimals ("0,0%") on a comma-decimal-locale machine — found via live smoke test against a real running `Host`, fixed with explicit `CultureInfo.InvariantCulture`.

## 15. Performance Graphs

- [x] 15.1 Vendor `chart.js` under `Presentation`'s `wwwroot` (no CDN reference) — `wwwroot/js/vendor/chart.umd.min.js` (4.4.7 UMD build, downloaded once and committed, not fetched at runtime)
- [x] 15.2 Build a minimal `IJSRuntime` interop wrapper: create a chart instance, push incremental points (`chart.data.datasets[...].data.push(...); chart.update('none')`), destroy on component dispose — `wwwroot/js/chart-interop.js` (JS side) + `Dashboard/Charts/IChartInterop.cs`/`ChartInterop.cs` (C# wrapper, interface split out so tests can substitute a spy instead of a real JS engine, same precedent as `IRunEventBus`/`IResourceEventBus`)
- [x] 15.3 Build host CPU/RAM graph components in the Status header, always live, backed by the host in-memory circular buffers — `<LiveResourceGraph SeriesKey="@ResourceHistoryBuffer.HostSeriesKey" />`, unconditionally rendered
- [x] 15.4 Build a per-run CPU/RAM graph component, lazily mounted only when that run's card is expanded, backed by that run's in-memory circular buffer — used for every sampled kind; a transfer's graph is visually labeled (e.g. "host activity during transfer") to distinguish it from a per-process-tree figure — same `LiveResourceGraph` component reused with `SeriesKey="@run.Id.ToString()"`, gated behind a per-run `_expandedRunIds` toggle in `Status.razor`
- [x] 15.5 Build a run-detail graph component for the run-detail subpage, backed by the full `RunResourceSample` series for that run (not windowed), for every sampled kind, with the same transfer-specific labeling as 15.4 — `RunHistoryGraph.razor`, one `IRunResourceSampleRepository.GetSeriesAsync` call, one `SetSeriesAsync` interop call per canvas (not incremental)
- [x] 15.6 BDD: host graphs render and extend live as new samples arrive, showing at least the trailing 10 minutes — implemented as a bUnit test (`StatusComponentTests.Host_resource_graph_is_created_on_render_and_extends_live_as_snapshots_arrive`), not a Reqnroll `.feature` file: same deviation already established for 12.12-12.15/13.19-13.21 — no Blazor-interactive browser test client in this project's toolchain, so a real Chart.js render can't be driven by `WebApplicationFactory`-based `BehaviorTests`. `SpyChartInterop` (shared with `RunDetailComponentTests`) stands in for the JS engine and records what would have been drawn
- [x] 15.7 BDD: an in-flight run's graph, once expanded, shows that run's own recent usage and continues updating live — for a command run and for a transfer — same bUnit deviation as 15.6: `A_runs_live_chart_is_not_created_until_its_card_is_expanded_and_is_destroyed_on_collapse` (command run) + `An_expanded_transfers_live_graph_shows_the_host_activity_note` (transfer)
- [x] 15.8 BDD: a completed run's history detail graph spans its entire recorded duration, including runs longer than 10 minutes, for a command run and for a transfer — same bUnit deviation: `RunDetailComponentTests.Renders_a_full_duration_resource_graph_spanning_longer_than_the_10_minute_live_window` (deliberately 15 minutes of samples, longer than the live buffer's 10-minute window, to prove this reads the persisted series not the buffer) + `Renders_a_completed_transfers_full_duration_graph_with_the_host_activity_note`
- [x] 15.9 Unit test: a run's live chart component is not instantiated (no interop calls, no per-run subscription) while its card is collapsed — same test as 15.7's command-run case: asserts `SpyChartInterop.CreatedCanvasIds` stays at 2 (host graph only) until the card is expanded, then jumps by 2, then drops on collapse (`DisposeAsync`)

**Real, pre-existing bug found and fixed while smoke-testing this section against a real running `Host` (not just `WebApplicationFactory`/bUnit): no static asset was ever actually reachable at runtime**, including `dashboard.css` and `blazor.web.js` itself — meaning no prior section's dashboard work had ever been verified in an actual browser with a live SignalR circuit, only via bUnit's simulated render pipeline and plain HTTP GETs of server-rendered HTML. Root cause and fix:
- `Program.cs` never called `app.MapStaticAssets()` (or `UseStaticFiles()`) at all — added.
- `App.razor` (the root HTML host document) lived inside `Presentation`, a Razor Class Library consumed by `Host`. Static web assets from a *referenced* RCL are composed under a `_content/{AssemblyName}/` prefix in the *consuming* app's manifest, not at their bare project-relative path — but `App.razor`'s hardcoded `<link>`/`<script>` paths (`css/dashboard.css`, `_framework/blazor.web.js`, etc.) assumed the bare path, which only resolves correctly when the app's root document lives in the actual entry-point project itself (the standard `dotnet new blazor` template always does this). Fixed by moving `App.razor` into `Host` (`src/OutOfTheBox.Host/App.razor` + a new `_Imports.razor`) and switching every static reference to the `@Assets["..."]` resolver (matching current .NET 10 template convention), which resolves the correct fingerprinted, correctly-prefixed URL at runtime. This in turn required `MapRazorComponents<App>().AddAdditionalAssemblies(typeof(Status).Assembly)` in `Program.cs`, since component discovery by default only scans `App`'s own assembly for `@page`-routable components, and every dashboard page still lives in `Presentation`. Also removed a dead `OutOfTheBox.Presentation.styles.css` `<link>` left over from the original template scaffold — Presentation has no scoped `.razor.css` files, so that bundle never existed and always 404'd silently.
- Verified via a live `dotnet run` smoke test (not just `curl`ing HTML): every asset referenced by the rendered `/login` page (`dashboard.css`, `blazor.web.js`, `chart.umd.min.js`, `chart-interop.js`) now resolves 200 with real byte content at its actual fingerprinted URL.

6 new UnitTests (3 `StatusComponentTests` + 3 `RunDetailComponentTests`, covering 15.6-15.9) backed by a new shared `SpyChartInterop` fake; every pre-existing `StatusComponentTests`/`RunDetailComponentTests` case updated to register `IChartInterop`/`ResourceHistoryBuffer`/`IRunResourceSampleRepository` now that `Status`/`RunDetail` depend on them. 124/124 UnitTests (118 prior + 6 new), 5/5 ArchitectureTests, 51/51 BehaviorTests (unchanged - no new BehaviorTests this section, per the bUnit deviation above) all pass; 0 build warnings.

## 16. Transport & Network

- [x] 16.1 Configure Kestrel to require HTTPS on the configured port — `Program.cs` fails fast at startup if any configured `Kestrel:Endpoints` entry isn't `https://` (defense against a future config mistake, on top of the fact no `Http` endpoint is or ever has been configured), plus `ConfigureHttpsDefaults` pins `SslProtocols` to TLS 1.2/1.3 only
- [x] 16.2 Document/generate the certificate used (self-signed acceptable for v1) and how the sbx-side client pins/trusts it — `INSTALL.md`'s new "Network & transport" section (`dotnet dev-certs`/`New-SelfSignedCertificate` generation, Kestrel `Certificate` config binding, `curl --cacert`/`HttpClientHandler.ServerCertificateCustomValidationCallback` pinning guidance)
- [x] 16.3 Document Windows Firewall inbound rule scoping the command-API port (covering `dotnet`, `git`, and artifact-transfer endpoints alike) to the sbx sandbox's IP, and the dashboard port/path to the operator's network — same `INSTALL.md` section, `New-NetFirewallRule` example scoped by `-RemoteAddress` to both the sbx sandbox and the operator (command API and dashboard share one port, so a port-level rule can't separate them - documented explicitly)
- [x] 16.4 Document required sbx-side client behavior for consuming SSE (e.g. `curl -N`, or HttpClient with `HttpCompletionOption.ResponseHeadersRead`) so responses aren't fully buffered before use — applies to `POST /run`/`POST /run/git`; note that `POST /artifacts` streams a binary body rather than SSE and document the corresponding non-buffering guidance for a plain file download — same `INSTALL.md` section, both cases documented separately (SSE framing vs. raw binary download), including the artifact-transfer path-confinement rejection behavior (distinct "not found" vs. "confinement violation" outcomes)

No new tests: 16.1's startup guard is inline `Program.cs` validation (like the rest of the file's DI/middleware wiring, not independently unit-tested in this project — implicitly exercised by every `BehaviorTests` run, whose `WebApplicationFactory` configures no `Kestrel:Endpoints` at all, so the loop is a no-op there and the real Kestrel binding path is only exercised by an actual `dotnet run`). Verified via a live `dotnet run` + `curl` smoke test that the service still starts and serves normally with the new startup check and TLS version pin in place. 124/124 UnitTests, 5/5 ArchitectureTests, 51/51 BehaviorTests (all unchanged from Section 15) pass; 0 build warnings. 16.2-16.4 are documentation-only, added as a new section in `INSTALL.md` rather than a separate file, since that's already this repo's designated "running/deploying the service" doc (per `README.md`'s Documentation index); also fixed a stale "Section 14 (Packaging & Install/Upgrade)" cross-reference in `INSTALL.md` left over from before the plan grew to 20 sections.

## 17. Packaging & Install/Upgrade

- [x] 17.1 Configure `Host` for self-contained, single-file, win-x64 publish (`--self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true`, trimming disabled); confirm publish targets only `src/Host/OutOfTheBox.Host`, not `Domain`/`Application`/`Infrastructure`/`Presentation` or the test projects individually (those are pulled in transitively as compiled dependencies and composed static web assets, not published as separate outputs) — `src/OutOfTheBox.Host/Properties/PublishProfiles/win-x64.pubxml`; verified via a real `dotnet publish src/OutOfTheBox.Host -p:PublishProfile=win-x64` producing one ~110 MB `OutOfTheBox.Host.exe` plus a `wwwroot/` folder (including `chart.umd.min.js` at its composed `_content/OutOfTheBox.Presentation/...` path) — no other project's own output appears as a separate publish target
- [ ] 17.2 Verify the published exe runs standalone on a clean Windows machine with no .NET runtime installed, but with `git` installed and on `PATH` (a new prerequisite alongside the OS itself) — **deferred to manual verification** (this dev environment has the .NET SDK installed, so "no .NET runtime present" can't be genuinely tested here); tracked alongside 17.10-17.12 below, all of which need a real second machine
- [x] 17.3 Define the data directory layout (e.g. `%ProgramData%\OutOfTheBox\`) holding config and the SQLite file, separate from the install directory — `Program.cs` now loads an optional `<dataDir>/appsettings.json` (data dir resolved from `OUTOFTHEBOX_DATA_DIR`, defaulting to `%ProgramData%\OutOfTheBox`) layered on top of the bundled defaults; verified live that a bearer token set only in the data-directory file is what the running service actually enforces
- [x] 17.4 Set up the MSI project (WiX v7, SDK-style, `installer/OutOfTheBox.Msi/OutOfTheBox.Msi.wixproj`, `dotnet build`-able, no Visual Studio) referencing the WiX Util and Firewall extensions; author the core `Package` definition (Id, Name, Version synced from `Directory.Build.props`'s `<Version>`, Manufacturer, a fixed/stable `UpgradeCode`) — deviation: WiX v7's global CLI tool gates on accepting the "Open Source Maintenance Fee" EULA (`wix eula accept wix7`/`<AcceptEula>wix7</AcceptEula>`), a real FireGiant licensing term (free unless the using organization has >$10k/year revenue) surfaced to and explicitly accepted by the user before proceeding, not accepted unilaterally; `installer/Directory.Packages.props` opts the `installer/` tree out of the repo's Central Package Management, since WiX's own package versioning conventions don't fit CPM cleanly
- [x] 17.5 Harvest/author `Component`/`File` elements covering the self-contained publish output (exe + `wwwroot`) installed to `[ProgramFilesFolder]OutOfTheBox`, sourced from the `win-x64` publish profile's output directory — deviation: `HarvestDirectory` (an MSBuild item from v3's `heat.exe`) isn't part of the modern SDK; used WiX v4+'s native wildcard `<Files Include="...**">` instead. Verified for real: extracted the built MSI's embedded cabinet (`wix msi decompile` + `expand.exe`) and confirmed all 30 harvested files are present with correct sizes, including `OutOfTheBox.Host.exe` at its exact 112,938,871-byte publish size - not just "the build succeeded"
- [x] 17.6 Author `util:User`/`util:Group` for the dedicated least-privilege service account (not local admin, not a pre-existing/shared account) — log-on-as-a-service granted automatically by service creation, explicit "Performance Monitor Users" membership — plus permissions on the data directory and configured repo root
- [x] 17.7 Author native `ServiceInstall`/`ServiceControl` for the Windows Service running as that account, plus `util:ServiceConfig` (or equivalent) for SCM crash-recovery restart actions (SCM does not restart a crashed service by default)
- [x] 17.8 Author `firewall:FirewallException` scoped to the configured port
- [x] 17.9 Author a config-collection `WixUI` dialog (repo root directory via folder browse, bearer token pre-filled with a randomly generated value and editable, port) feeding MSI properties, plus a deferred custom action rendering those properties into the data directory's `appsettings.json` (the same file `Program.cs`'s `OUTOFTHEBOX_DATA_DIR` handling already reads — no new config-loading mechanism needed); verify `chart.js` exists at its expected installed path as part of the same custom-action pass — **done, with a deviation on the last part**: config is written into the service's `Environment` registry value, not a custom-action-rendered `appsettings.json` (reuses `Program.cs`'s existing environment-variable override, no need for the custom-action DLL to also handle JSON rendering). The bearer token *does* now get an auto-generated random default (see 17.9a below) - the "not worth a custom-action DLL" call from when this task was first attempted was reversed once the follow-up request specifically asked for it, and turned out to be needed anyway once a second latent bug surfaced (17.9a). `ConfigDlg` itself required a **real correctness fix along the way**: authoring it in its own `Fragment`/file compiled clean but produced an MSI with *no Dialog/Control table at all* - WiX silently dead-code-eliminates a Fragment nothing references, and `wix msi decompile` doesn't surface UI tables either, so this was caught only by querying the built MSI's `Dialog`/`ControlEvent` tables directly via the Windows Installer COM API. Fixed by moving the UI content directly under `<Package>` in `Package.wxs` (always-included, no reference needed) rather than a separate file. Re-verified the same way afterward: `ConfigDlg` present in the `Dialog` table, `WelcomeDlg`/`VerifyReadyDlg`'s Next/Back `ControlEvent` overrides present with the expected `Order`/`Condition`, and the three `Edit` controls correctly bound to `REPOROOTDIR`/`BEARERTOKEN`/`PORTNUMBER`. The `chart.js`-exists post-install verification is still not authored (a smaller, separate follow-up). **Resolved (see 17.9e)**: Burn's `WixStandardBootstrapperApplication` ran the chained `MsiPackage` at a reduced UI level by default, so `ConfigDlg` never displayed when installing through the bootstrapper - confirmed for real by the operator's own install attempt ("no choices, just Install or Cancel"), not just theorized. Fixed via `bal:DisplayInternalUICondition`, not the non-existent v4+ `MsiPackage` attribute this task originally (correctly) ruled out
- [x] 17.9a *(added)* Auto-generate the bearer token's default (cryptographically random, editable, preserved across upgrade unless explicitly overridden) — `installer/OutOfTheBox.Msi.CustomActions` (a genuine managed custom action DLL, `WixToolset.Dtf.CustomAction`/`WixToolset.Dtf.WindowsInstaller`, net472 so it doesn't depend on the .NET runtime this installer exists to establish, auto-wrapped into an MSI-loadable `.CA.dll` by the SDK's own build targets) resolves both `BEARERTOKEN` and `SERVICEACCOUNTPASSWORD` with the same three-way precedence: an explicit value already on the property (command-line override) wins; else a value found via `RegistrySearch` into a persisted copy under `HKLM\SOFTWARE\OutOfTheBox` (an upgrade) wins; else generate a new cryptographically random one (`RandomNumberGenerator`, not a weaker PRNG). Scheduled after `AppSearch` in both `InstallUISequence` (so `ConfigDlg` shows the resolved value) and `InstallExecuteSequence` (so a fully silent install/upgrade still resolves it). The resolved values are written back to that same `HKLM\SOFTWARE\OutOfTheBox` location by a new `PersistedSecretsComponent`, closing the loop for the *next* upgrade. **Found and fixed a real bug while implementing this, before it ever shipped**: `SERVICEACCOUNTPASSWORD` (and `SERVICEACCOUNTNAME`/`SERVICEACCOUNTDOMAIN`) were referenced throughout `Package.wxs` (`util:User`'s `Password="[SERVICEACCOUNTPASSWORD]"`, `ServiceInstall`'s `Account="[SERVICEACCOUNTDOMAIN]\[SERVICEACCOUNTNAME]"`) but **never actually defined anywhere** - the MSI built and validated cleanly regardless, but would have created the service account with an empty/undefined password, meaning the service could never actually log on. Fixed by defining `SERVICEACCOUNTNAME`/`SERVICEACCOUNTDOMAIN` as fixed properties and folding the password into the same generate-or-preserve mechanism as the bearer token. **Also found and fixed a precedence bug in the custom action's own first draft**: it checked the persisted value before the current (possibly command-line-supplied) one, meaning an operator explicitly overriding the bearer token during an upgrade would have been silently discarded in favor of the old stored value - the exact opposite of "make sure the user can still change it if desired." Caught by reasoning through the upgrade-with-override scenario, not by a tool. The precedence logic was extracted into a pure, `Session`-independent function (`SecretResolution.Resolve`) specifically so this class of bug has automated coverage going forward - see `installer/OutOfTheBox.Msi.CustomActions.Tests` (11 xUnit tests, including the exact scenario that was wrong: `Resolve_prefers_an_explicit_current_value_over_a_stored_one`). Verified end-to-end via the Windows Installer COM API: `CustomAction`/`InstallUISequence`/`InstallExecuteSequence`/`Property`/`Registry`/`Binary` tables all inspected directly, not just "the build succeeded."
- [x] 17.9b *(added)* Show the product version (and, on an upgrade, the version being upgraded from) in the installer UI, and give the product a consistent visual brand across the installer and the dashboard — `ConfigDlg`'s title now reads `Configure OutOfTheBox v[ProductVersion]` (`ProductVersion` is a built-in MSI property auto-derived from `Package/@Version`, no explicit definition needed); a new conditionally-shown `UpgradeNotice` control reads `Upgrading from version [EXISTINGVERSION]`, backed by a new `EXISTINGVERSION` `RegistrySearch` property (same `HKLM\SOFTWARE\OutOfTheBox`-during-`AppSearch` pattern as `EXISTINGBEARERTOKEN`/`EXISTINGSERVICEACCOUNTPASSWORD`) and shown/hidden via `ShowCondition`/`HideCondition` attributes on `<Control>` (the WiX v4+ mechanism — the v3-era `<Condition Action="show/hide">` child-element form no longer exists); `PersistedSecretsComponent` now also writes `Version=[ProductVersion]` so the *next* upgrade's `EXISTINGVERSION` search finds it. Brand mark: a terminal-prompt `>` chevron bursting from an orange-to-pink gradient box (rendered programmatically via `System.Drawing.Common`, no image tooling available in this environment — a hand-rolled multi-resolution ICO writer was needed too, since `System.Drawing`'s own `ImageFormat.Icon` save is known-unreliable for proper multi-res output), applied consistently: `installer/Assets/outofthebox.ico` (bootstrapper exe icon via `Bundle/@IconSourceFile`, MSI Add/Remove Programs icon via `ARPPRODUCTICON`/`<Icon>`), `banner-493x58.bmp`/`dialog-493x312.bmp` (WixUI's well-known `WixUIBannerBmp`/`WixUIDialogBmp` variable ids, replacing the stock placeholder art at WixUI's own fixed dimensions), `logo-96.png` (bootstrapper's inline `LogoFile`), and on the dashboard: `favicon.ico` plus a `logo.png` shown in the header (`DashboardLayout.razor`) and login page (`Login.razor`), with `dashboard.css`'s `--accent` retuned to the mark's warm gradient end and a new `--brand-indigo` used for the header bar background. Verified end-to-end against the compiled MSI via the Windows Installer COM API (not just "the build succeeded"): `Icon`/`Property(ARPPRODUCTICON)`/`Binary` tables confirm the icon and both WixUI bitmaps are embedded; `Control`/`ControlCondition` tables confirm `Title`'s and `UpgradeNotice`'s exact text and show/hide conditions; `AppSearch`/`Registry` tables confirm `EXISTINGVERSION`'s search and the new `Version` persistence row. Main .NET test suite (124/124 UnitTests, including the bUnit component tests covering `DashboardLayout`/`Login`) and `installer/OutOfTheBox.Msi.CustomActions.Tests` (11/11) both still pass unchanged — no test logic needed updating, only markup/styling.
- [x] 17.9c *(added)* Extend the dashboard's dark theme to the installer UI, on explicit request ("make the installer style dark like the dashboard") — scoped upfront via a direct question to the user given a real technical ceiling: native MSI dialogs are actual Win32 dialogs, and Edit/PushButton/CheckBox controls are OS-drawn with no MSI table for recoloring them, so pixel-parity with the Blazor dashboard is not achievable. Chosen scope: "dark chrome, light inputs" - darken every reachable dialog's background/text, leave native input controls alone. `ConfigDlg`/`InvalidConfigDlg` (both fully owned) got a full treatment: a new full-dialog `Bitmap` control (referencing a `Binary`, same mechanism as an `Icon` control) as the first-declared control so it paints behind everything else, three new `<TextStyle>` rows (`OOTB_Title`/`OOTB_Body`/`OOTB_Muted`, light-colored to match `dashboard.css`'s `--text`/`--text-muted`) referenced via MSI's `{\StyleId}` text-prefix mechanism, and `Transparent="yes"` added to every static Text control so it reads against the bitmap instead of sitting in an opaque white box. The old `BannerLine`/`BottomLine` divider `Line` controls were dropped (no MSI mechanism to recolor a `Line`, and a default-colored one would look wrong against a dark background). Three background bitmaps (`installer/Assets/panel-370x290.bmp`/`panel-370x270.bmp`/`panel-260x85.bmp`, one per distinct dialog aspect ratio in play since `Bitmap` controls always stretch-to-fit) were rendered programmatically as a soft, low-opacity echo of `dashboard.css`'s own body-background radial-gradient wash (violet upper-left, pink upper-right, over the same `#14161a`) - `System.Drawing`'s `PathGradientBrush` showed real, visible concentric banding artifacts at these radii/opacities (a genuine GDI+ limitation, not a tuning miss), worked around by compositing many thin, low-alpha concentric `FillEllipse` layers instead. `WelcomeDlg`/`ExitDialog` needed no changes: WixUI_Minimal's own "dialog style" layout already covers ~90% of their area with the existing branded `WixUIDialogBmp`. **A genuine attempt to extend the same treatment to WixUI_Minimal's own stock dialogs (`CancelDlg`, `ProgressDlg`) failed outright** - redeclaring a `<Dialog>`/`<Control>` with the same Id as one pulled in from the referenced `WixUI_Minimal` wixlib is a hard `WIX0091` "Duplicate Control/Dialog" linker error, not a supported override mechanism (verified by actually trying it, not assumed); WiX has no partial-override path for an individual stock dialog short of hand-authoring the entire dialog set from scratch. Given that, and given `VerifyReadyDlg` specifically has ~25 controls and ~40 `ControlEvent` rows spanning disk-space-exhaustion and modify/repair/update states this installer never exposes a path to, every other stock WixUI_Minimal dialog was deliberately left with its default light styling - reskinning it isn't worth the regression risk to a screen seen for a few seconds. Verified against the compiled MSI via the Windows Installer COM API: `Binary` table has all three panel bitmaps; `TextStyle` table's `Color` values (COLORREF/BGR-packed) match the intended RGB exactly; `Control` table confirms `ConfigDlg`/`InvalidConfigDlg`'s `Background`/text rows carry the right `{\StyleId}`-prefixed text; `CancelDlg` confirmed still exactly stock (sanity check that the failed override attempt left no partial damage behind). `wix msi validate` passes; full .NET test suite unaffected (no `.cs` changes, WiX authoring + new binary assets only).
- [x] 17.9e *(added)* Fix `ConfigDlg` never displaying when installing through the bootstrapper (only via `msiexec /i` directly) - a real, operator-reported bug ("in the installer, i do not see the version when installing. i also get no choices, i can only install or cancel"), not a hypothetical. Root cause exactly matched what 17.9's own open question had already flagged: Burn's `WixStandardBootstrapperApplication` runs a chained `MsiPackage` at a reduced UI level by default. Fixed via `bal:DisplayInternalUICondition="WixBundleAction = 6"` on the `MsiPackage` in `Bundle.wxs` - confirmed via web research (WiX's own FAQ plus a maintainer's direct response) that v3's `DisplayInternalUI` was relocated into the `bal:` namespace for v4+ rather than removed, and that it's fully supported by the *standard* BA specifically (not only a custom one), before writing any code - avoided repeating the earlier CancelDlg/ProgressDlg override mistake of assuming first and finding out via a build error. `WixBundleAction = 6` (`BOOTSTRAPPER_ACTION_INSTALL`) scopes it to install/upgrade only, not uninstall/modify/repair. Verified against the compiled bundle, not just "the build succeeded": `wix burn extract` on the rebuilt `.exe`, confirming `BootstrapperApplicationData.xml` carries `<WixBalPackageInfo PackageId="OutOfTheBoxMsiPackage" DisplayInternalUICondition="WixBundleAction = 6" />` exactly - the file is UTF-16 encoded, and a first grep attempt against it as UTF-8 found nothing, a false negative worth remembering for next time.
- [x] 17.9f *(added)* Rename the product's *display* name to "Out of the Box" (spaced, human-written) everywhere an operator actually reads it - installer dialogs (via `[ProductName]`, cascading automatically through every stock WixUI_Minimal dialog, not just `ConfigDlg`), Programs and Features, the Services console (`ServiceInstall/@DisplayName`, kept distinct from `@Name`, the technical SCM key other things still reference by exact match), the Windows Firewall rule list, and every dashboard page/title/the `/version` endpoint (via a new single `VersionInfo.DisplayName` constant, not a literal string duplicated per file) - on explicit request, since "OutOfTheBox" is "fine in code and other files the end user will never see, and for the installer file name" but not everywhere else. Everything script/code-referenced by exact string (project/namespace names, registry key paths, environment variable names, the service account name, `INSTALLFOLDER`'s actual path, every file name including `OutOfTheBoxSetup.exe`) deliberately kept the unspaced identifier - see design.md's "Product display name" decision for the exact dividing line and reasoning. Updated the two BDD step assertions that hardcoded the old string (`ServiceDashboardSteps.cs`) and every doc that quotes the literal installer/dashboard text (`INSTALL.md`, `README.md`, `SKILL.md`) to match. Verified via the Windows Installer COM API (`Property(ProductName)`, `ServiceInstall.DisplayName`, the firewall extension's own table, `ConfigDlg`'s `Title` control text) against the compiled MSI, and the full test suite (124/124 UnitTests, 5/5 ArchitectureTests, 51/51 BehaviorTests) after updating the two now-stale assertions.
- [x] 17.9g *(added)* 17.9f's rename missed the two WixUI brand bitmaps: `installer/Assets/banner-493x58.bmp` and `dialog-493x312.bmp` have "OutOfTheBox" (plus, on the dialog image, a "dotnet & git, out of the sandbox" tagline) rendered as literal pixels, not text a MSI property could substitute into - `[ProductName]` cascades through dialog *text* controls, not into a bitmap. Missed initially because only `logo.png` (the dashboard's small brand mark, pure graphic with no baked-in text) was checked before generalizing "no text in the assets" to all of them; the operator caught it directly ("dialog-493x312 still contains the internal name"). Fixed by regenerating both bitmaps from scratch with a PowerShell/`System.Drawing` script (the original ad hoc "LogoGen"/"PanelGen" console apps from 17.9b were already cleaned up, so this was rewritten rather than reused): same diagonal indigo-to-violet linear gradient sampled pixel-for-pixel from the originals (`#080713` top-left to `#4C1D95` bottom-right), the existing `logo-96.png` composited in unchanged (so the icon mark stays pixel-identical everywhere it appears), with "Out of the Box" (and the unchanged tagline) redrawn in Segoe UI Bold. `outofthebox.ico`, `logo-96.png`, and the three `ConfigDlg`/`InvalidConfigDlg` panel bitmaps (`panel-370x290.bmp`/`panel-370x270.bmp`/`panel-260x85.bmp`) were individually re-checked too and confirmed to be pure graphics/gradient washes with no baked-in text - no change needed there. Verified the rebuilt MSI's `Binary` table still carries `WixUI_Bmp_Dialog`/`WixUI_Bmp_Banner` rows (both `dotnet build` passes re-embed the source file bytes directly, no separate caching layer for bitmaps to go stale) and confirmed the new dimensions/pixel format (493x312 and 493x58, both 24bpp, matching the originals exactly) on the source files before rebuilding.
- [x] 17.10 Author a `Launch` condition verifying `git` is resolvable on `PATH`, failing the install with a clear message if not (defense in depth — the bootstrapper below is supposed to have already guaranteed it, but the MSI can be run directly, bypassing the bootstrapper) — checks `HKLM\SOFTWARE\GitForWindows\InstallPath`
- [x] 17.11 Author `MajorUpgrade` for in-place upgrade; ensure the data directory is never referenced by any `Component`/`RemoveFolder` in the MSI at all, so it survives both upgrade and uninstall structurally, not just by policy — confirmed by inspection: `DATADIRFOLDER`'s only `Component` (`DataDirectoryComponent`) exists solely to set permissions via `CreateFolder`/`util:PermissionEx`, and no `Component` anywhere references a file under it
- [x] 17.12 Set up the Burn bootstrapper project (`installer/OutOfTheBox.Bootstrapper/OutOfTheBox.Bootstrapper.wixproj`) chaining, in order: the .NET 10 SDK (official Microsoft redistributable package reference, detected via registry/product search, downloaded and installed only if a compatible version isn't already present), Git for Windows (same detect-then-install-only-if-missing pattern), then the MSI from above — required because the spawned `dotnet build`/`test`/`git` processes this service exists to run need the SDK/git on `PATH` regardless of `Host`'s own self-contained packaging. Both prerequisite packages' download URLs and hashes were independently verified by actually downloading both installers (~204 MB + ~65 MB) and computing their hashes with `Get-FileHash`, not copied from a page and trusted, though .NET SDK patch releases mean this pinned version will eventually need a refresh (documented in a `Bundle.wxs` comment). **A real bug shipped here and was only caught by an actual failed install on a real machine (17.18)**: the "independently verified" claim above was only half true - the SDK payload's hash was correctly captured as SHA-512 (128 hex chars), but the Git for Windows one was captured as SHA-256 (64 hex chars, `Get-FileHash`'s default `-Algorithm`), an inconsistency between the two capture commands. `ExePackagePayload/@Hash` must always be SHA-512 - Burn computes/compares SHA-512 unconditionally regardless of what's supplied, so a SHA-256 value doesn't error at build time or under `wix msi validate`/ICE (neither checks payload hashes against anything at all), it just fails verification on a genuine install attempt with a length-mismatched "actual" hash in the Burn log. Root-caused from the operator's own real install log (`e000: Error 0x80091007: Hash mismatch`, expected 64 hex chars vs. actual 128), confirmed by re-downloading the exact same file and computing both algorithms (the SHA-256 matched the wrong stored value exactly; the SHA-512 matched Burn's reported "actual" exactly, proving the download itself was never the problem), and fixed by recapturing with `-Algorithm SHA512` explicitly. Verified the fix landed in the actual compiled artifact, not just "the build succeeded": `wix burn extract` on the rebuilt bundle, confirming its embedded `manifest.xml` carries the corrected 128-char hash exactly.
- [x] 17.13 Remove `scripts/install.ps1`/`scripts/upgrade.ps1` (superseded by the bootstrapper/MSI)
- [x] 17.14 Document the dedicated service account the MSI creates (not local admin, not a pre-existing/shared account) and exactly what rights it's granted and why — `INSTALL.md`'s "Production install" section, step 3, rewritten for the bootstrapper/MSI flow
- [x] 17.15 Document that the run registry is in-memory only: a restart drops all lock/run-id state (cancel calls for pre-restart run ids will 404; repos locked before restart become immediately available; SQLite rows reconcile to `Interrupted`) — applies to every run kind alike — `INSTALL.md`'s "After a restart" subsection (unaffected by the WiX pivot, no change needed)
- [x] 17.16 Document that the data directory (config + SQLite file) is untouched by upgrade and by uninstall/reinstall, so history and configuration survive all three — `INSTALL.md`'s "Production install" section, steps 3-4, rewritten for the bootstrapper/MSI flow (now also explicitly covers uninstall, which `install.ps1`/`upgrade.ps1` never had to address)
- [x] 17.17 Document that downgrade is unsupported (migrations are forward-only) and recommend operators copy the SQLite file before upgrading if they want a manual rollback point — `INSTALL.md`'s "Production install" section, step 4 (unaffected by the WiX pivot, no change needed)
- [ ] 17.18 Manual test: the bootstrapper on a clean host (no .NET SDK, no git) installs both prerequisites, then results in a running, reachable, authenticated service with an empty history, working resource monitoring (confirming the Performance Monitor Users grant took effect), and working graphs (confirming `wwwroot`/`chart.js` was installed) — **deferred**, needs a real second Windows machine and operator-present execution of an installer that creates real system-level state (a local service account, a Windows Service, a firewall rule, possibly a real SDK/git install). **Also the place to resolve 17.9's open question**: confirm whether `ConfigDlg` actually displays when installing through the bootstrapper, or whether the run is silent and relies on command-line `MsiProperty` pass-through as currently documented in `INSTALL.md`
- [x] 17.18a *(added)* Fix `ResolveSecrets` (the managed custom action from 17.9a) failing on a genuine first real-machine install attempt of 17.18 - `Error 0x80070643`/generic 1603 in the bootstrapper's own log, root-caused via the operator-provided per-package MSI log (`..._002_OutOfTheBoxMsiPackage.log`, not visible in the bundle's own top-level log) to `SFXCA: Failed to get requested CLR info. Error code 0x80131700` on `ResolveSecrets` itself, despite the test machine being a fully current Windows 11 24H2 install with .NET Framework 4.8.1 present - not a missing-Framework problem, a real one-line gap. Root cause: `OutOfTheBox.Msi.CustomActions.csproj` had no `CustomAction.config`, and `WixToolset.Dtf.CustomAction`'s `PackCustomAction` MSBuild target (confirmed by reading its `.targets` file directly rather than assuming) only embeds `@(Content)` items into the `.CA.dll` SFXCA package - nothing auto-generates one. Without an explicit `CustomAction.config`, `SfxCA.dll`'s in-process CLR host has to guess which runtime to load, and that guess fails on some real machines even with a fully current Framework installed - a decades-old, still-load-bearing WiX/DTF quirk traced to the identical error message and identical fix (`useLegacyV2RuntimeActivationPolicy="true"` alongside `<supportedRuntime version="v4.0" />`) in WiX's own issue tracker going back to WiX 3.5/2010, confirmed via WebSearch/WebFetch against the actual maintainer response before writing the fix, not assumed. Fixed by adding `installer/OutOfTheBox.Msi.CustomActions/CustomAction.config` and marking it `<Content Include="CustomAction.config" />` in the `.csproj` (must be `Content`, not `None` - `None` items are never picked up by `PackCustomAction`). Verified the file is actually packaged into the rebuilt `.CA.dll` (its filename appears in a plain-text scan of the binary; its XML content doesn't, expected since `MakeSfxCA` stores package contents in a compressed CAB, not inline) - full pattern this task series established ("verify against the compiled artifact," not just "the build succeeded"), short of an actual second-machine re-install which remains 17.18's own deferred scope. `installer/OutOfTheBox.Msi.CustomActions.Tests` (11/11) unaffected - the bug was in SFXCA packaging/CLR hosting, not `SecretResolution`'s own logic.
- [x] 17.18b *(added)* Second real-machine round (17.18, on Windows Server 2025 Datacenter via a Hyper-V VM, checkpoint-reset between each attempt) found three more real issues, none catchable by a clean build alone - a `ResolveSecrets` fix that actually worked this time (17.18a), confirmed by the install reaching all the way to a genuine post-install service-start failure instead of aborting mid-MSI:
  - **Bootstrapper showed license boilerplate the operator never wanted**: `Bundle.wxs`'s `Theme="hyperlinkLicense"`/`LicenseUrl=""` was believed (per WiX's own docs, e.g. FireGiant's "Specifying the WiX Standard Bootstrapper Application License") to suppress the license hyperlink/checkbox entirely when the URL is empty - a real install showed "I agree to the license terms and conditions [license terms]" regardless, contradicting the documented behavior. WixStdBA has no per-control override for a stock theme (same `ThemeFile`-or-nothing constraint as every other stock-dialog limitation this project has hit) - fixed by extracting the actual compiled `HyperlinkTheme.xml`/`.wxl` (found via `obj/.../wix-ir/`, the real intermediate WiX itself compiles from, not guessed at) into local `installer/OutOfTheBox.Bootstrapper/Theme.xml`/`Theme.wxl` copies with the `EulaHyperlink`/`EulaAcceptCheckbox` controls and their now-unused localization strings deleted, wired in via `ThemeFile`/`LocalizationFile` on `bal:WixStandardBootstrapperApplication`. `Theme="hyperlinkLicense"`/`LicenseUrl=""` had to stay too - WiX's own schema validation requires both `@Theme` and exactly one of `@LicenseFile`/`@LicenseUrl` to have *a* value even when a custom theme overrides what's actually rendered. Verified via `wix burn extract` on the rebuilt bundle: the embedded `thm.xml` carries this file's own added comment (proof it's the edited copy, not the stock one) and no `Hypertext Name="EulaHyperlink"`/`Checkbox Name="EulaAcceptCheckbox"` tags remain.
  - **The service failed to start after a genuinely clean install** - Event Viewer: "Logon failure: the user has not been granted the requested logon type at this computer" / "This service account does not have the required user right 'Log on as a service.'" 17.6's own comment claimed this right was "granted automatically once the service below is installed to run as this account" - that claim was flatly wrong: `CreateService`/MSI's `ServiceInstall` table never grants `SeServiceLogonRight` programmatically, only the interactive Services (`services.msc`) GUI does that as a side effect, which is irrelevant to an unattended MSI install - a genuine first-real-install-only bug this project's own COM-API table-inspection verification discipline could never have caught, since nothing about the *tables* was wrong. Fixed by adding `LogonAsService="yes"` to `util:User` (confirmed present in `WixToolset.Util.wixext` 7.0.0 via a binary string scan before use), which calls `LsaAddAccountRights` directly as part of account creation rather than relying on `ServiceInstall`'s own sequencing. Verified via `wix msi decompile`: the rebuilt MSI's `Wix4User` table attributes bitmask decompiles back out to a `<User>` element with `LogonAsService="yes"` present, not inferred from the raw bitmask by hand.
  - **`VerifyReadyDlg`'s stock black title text became unreadable once `banner-493x58.bmp` got a dark violet gradient (17.9b)** - this stock WixUI_Minimal dialog draws its own default black `InstallTitle`/`ChangeTitle`/etc. text directly on top of the shared `WixUIBannerBmp`. A first fix attempt lightened the banner instead of touching the text - **operator-rejected**: "restore it. the banner was fine. remove the TEXT that overlays the banner," a direct correction that the banner's own look (which the operator liked) shouldn't be the thing that changes. Two more surgical removal attempts were tried and both confirmed unsupported by WiX's schema before falling back to a full rewrite: a standalone top-level `<ControlCondition Dialog="VerifyReadyDlg" Control="InstallTitle" Action="hide" Condition="1" />` (mirroring the already-working standalone `<Publish Dialog="..." Control="...">` pattern this file uses to hook into WelcomeDlg/VerifyReadyDlg's Next/Back without redeclaring them) failed with `WIX0005` "unexpected child element" - `<UI>` only accepts a standalone `<Publish>`, not a standalone `<ControlCondition>`. Given operator input specifically requested the heavier option (full custom dialog, accepting the added risk), `banner-493x58.bmp` was restored to its dark, humanized version (`git show 8e41d21:...`, not regenerated) and a full replacement dialog, `VerifyReadyThemedDlg`, was hand-authored under a new Id - its entire Control/Publish content extracted via `wix msi decompile` against this project's own compiled MSI (not reconstructed from memory: the five install/change/repair/remove/update states each have their own disk-space-exhaustion sub-branches, real logic worth copying exactly rather than approximating), themed the same way `ConfigDlg`/`InvalidConfigDlg` already are (full-dialog `Background` Bitmap, `{\OOTB_Title}`/`{\OOTB_Body}` styled `Transparent="yes"` text, no Banner/Line controls), reusing `panel-370x270.bmp` - an asset already generated back in 17.9c for this exact dialog size and left unreferenced ever since. Native PushButtons kept exactly as stock, per the established "dark chrome, light inputs" scope. **Every reachable path into the stock dialog had to be found and retargeted, not just the obvious one**: querying the compiled `ControlEvent` table for every remaining `NewDialog -> VerifyReadyDlg` row (rather than assuming `ConfigDlg.Next` was the only entry point) turned up two more - `WelcomeDlg`'s patch-install case and, more importantly, `MaintenanceTypeDlg`'s Repair/Remove buttons (the real Programs-and-Features repair/uninstall entry points, which would otherwise have kept showing the unreadable stock dialog even after this fix). All three retargeted via the same standalone `<Publish Dialog="..." Control="...">` override pattern, at `Order="10"` (safely above any native or already-authored value on the same Dialog/Control pair - confirmed via a direct `Ordering`-sorted query of the compiled table, not assumed). Verified end-to-end against the compiled MSI: `VerifyReadyThemedDlg`'s 22 controls present with the right styled text, `ConfigDlg.Next`'s target updated, and all three other entry points' `Ordering=10` override rows present alongside (not replacing) the original `Ordering=1` native ones, confirming the override wins without deleting anything from the still-present, now-fully-unreachable stock `VerifyReadyDlg`.

  Full test suite (124/124 UnitTests, 5/5 ArchitectureTests, 11/11 CustomActions.Tests) unaffected by all three - WiX authoring and image-asset changes only, no `.cs` changes outside the already-verified `CustomAction.config` fix.
- [x] 17.18c *(added)* **The license fix above (17.9f's bootstrapper Theme.xml) was real but incomplete** - a third test round reported the exact same symptoms after a from-scratch clean rebuild ruled out a stale-artifact explanation. Root cause, found only by finally successfully querying the compiled `InstallUISequence` table directly (an earlier attempt failed on a PowerShell quoting bug and was never retried - every previous "no license text in the MSI" check had been run against `WelcomeDlg`'s `Control` table specifically, which genuinely has none): **`WelcomeDlg` is not WixUI_Minimal's fresh-install entry point at all** - `WelcomeEulaDlg` is (`Condition="NOT Installed"`; `WelcomeDlg`'s own row is `Condition="Installed AND PATCH"`, essentially dead since this project authors no `.msp`). `WelcomeEulaDlg` carries WixUI_Minimal's own default placeholder license text verbatim (`Control Id="LicenseText"`, literal RTF containing "Lorem ipsum dolor sit amet..." - the exact phrase the operator had been describing from the very first report), and its own `Install`/`InstallNoShield` buttons (not a `Next`) end the dialog sequence immediately once the license checkbox is checked - bypassing `ConfigDlg` entirely regardless of any `ControlEvent` wiring elsewhere in this file. This also retroactively explains the *original* "i get no choices, i can only install or cancel" report from earlier in this section: it was never only Burn's reduced-UI-level behavior (17.9e) - `WelcomeEulaDlg`'s own direct-to-Install path was always going to skip `ConfigDlg` regardless. Fixed via a standalone `<InstallUISequence>`/`<Show>` override at `Package`/`UI` scope: `<Show Dialog="override WelcomeEulaDlg" Condition="0" />` suppresses it outright (the `override` access modifier is the exact fix WiX's own `WIX7009` error names for a "virtual symbol" conflict - the modern replacement for v3's `Overridable="yes"` attribute); a second attempt to redirect `WelcomeDlg`'s own row to also cover `NOT Installed` hit a *different* conflict (`WIX0091`, against `WixUI_Minimal.wxs`'s own internal duplicate declaration of that same row) - rather than fight that too, a brand-new `<Show Dialog="ConfigDlg" Condition="NOT Installed" />` entry was added instead (no conflict, since `ConfigDlg` was never previously an `InstallUISequence` entry point), sending a fresh install straight into config collection with no Welcome step at all. Verified against the compiled MSI's actual `InstallUISequence` table, not assumed: `WelcomeEulaDlg`'s row now reads `Condition=0`, `ConfigDlg`'s new row reads `Condition=NOT Installed`, `WelcomeDlg`'s original `Installed AND PATCH` row is untouched. Full test suite (124/124 UnitTests, 5/5 ArchitectureTests, 11/11 CustomActions.Tests) unaffected. **Process lesson**: after two rounds of "I fixed it" not landing for the operator, the right move was a genuinely from-scratch clean rebuild (`rm -rf artifacts` plus each WiX project's own `bin`/`obj`, re-publish `Host`, rebuild MSI then Bootstrapper in order) to rule out any local build-staleness explanation *before* re-diagnosing - confirmed clean via `wix burn extract` + COM API against that from-scratch output, which is what led to actually re-querying `InstallUISequence` and finding the real gap instead of re-guessing at the same wrong dialog a third time.
- [x] 17.18d *(added)* Now that the license/entry-dialog bug (17.18c) no longer aborts the install, the service itself started for the first time and crashed immediately - Event Viewer/a crash dump: `Microsoft.Data.Sqlite.SqliteException: SQLite Error 1: 'no such table: Runs'` inside `EfRunRepository.ReconcileInterruptedAsync`, called right after `dbContext.Database.Migrate()` in the *same* startup scope. Root cause was not a migration bug (the same class of gotcha already documented from Section 15's dev-smoke-testing, but never before hit on a real deployed install): the bundled `appsettings.json` ships `SqliteFilePath: ""` deliberately (so a `WebApplicationFactory` test's in-memory config override is never shadowed by a bundled default), and the installer's `ServiceInstall` `Environment` registry value - which carries every other operator-configured/derived setting (`OUTOFTHEBOX_DATA_DIR`, `OutOfTheBox__RootDirectory`, `OutOfTheBox__BearerToken`, the Kestrel port) - never set `OutOfTheBox__SqliteFilePath` at all. An empty SQLite `Data Source` opens a fresh anonymous private database *per connection*, not one shared file - migrations landed on one such throwaway connection during startup, then `ReconcileInterruptedAsync`'s own query opened a different one with no schema at all. This means **every real installation of this service, from the very first one, would have crashed on startup this exact way** - the WiX dialog bugs (17.18a-c) simply prevented any install from ever running long enough to reach this. Fixed by adding `OutOfTheBox__SqliteFilePath=[DATADIRFOLDER]outofthebox.db` to the `Environment` multi-string - `outofthebox.db` was already the filename `INSTALL.md`'s own upgrade-rollback guidance assumed existed in the data directory, even though nothing had ever actually created it there. Full test suite (124/124 UnitTests, 5/5 ArchitectureTests, 11/11 CustomActions.Tests) unaffected.
- [x] 17.18e *(added)* **17.18d's fix didn't actually work** - the operator reported the identical crash again against a freshly-verified build. Root cause was much deeper than a missing entry: the `Environment` `RegistryValue`'s single `Value` attribute joined all five `NAME=value` pairs with a literal tab character (`&#x9;`), but a Windows Installer `Type="multiString"` `RegistryValue` does **not** split on tabs (or any whitespace) - the engine's own documented convention is the literal sequence `[~]` between entries. Confirmed by reading the actual compiled `Registry` table's raw character codes one-by-one (not just the rendered string, which visually looked like separate entries but wasn't): a genuine `[~]` marker appeared only at the very start and very end of the value, with a raw tab (character code 9) sitting between each pair - meaning the *entire* multi-line value was being written to the real Windows registry as **one single string** containing embedded tabs, not five distinct `REG_MULTI_SZ` array entries. This means the Windows Service's own per-service environment-block parsing (which requires each array element to be its own complete `NAME=value` string) would never have correctly split this into separate variables at all - **not just `SqliteFilePath`, but `OUTOFTHEBOX_DATA_DIR`, `RootDirectory`, `BearerToken`, and the Kestrel port too, none of them were ever actually reaching the running process as intended**, since Section 17.9 first authored this line. It went unnoticed because no install had ever run far enough to prove any of it working end-to-end - the COM-API verification used throughout this task series had only ever checked "does the row contain the right literal text," never "does this actually parse into distinct environment variables by the OS," which are different questions for a multi-string value. Fixed by switching from the single-`Value`-attribute form to WiX's proper multi-entry authoring - five `<MultiString Value="..." />` child elements under one `<RegistryValue Type="multiString">` (the WiX v4+/v5+/v7 element name; the similarly-named but deprecated `MultiStringValue` from v3 docs was tried first and rejected by the compiler). Verified by reading the compiled `Registry` table's raw character codes again: `[~]` now appears correctly *between* every entry, no stray tabs anywhere, no leading/trailing marker duplication. Full test suite (124/124 UnitTests, 5/5 ArchitectureTests, 11/11 CustomActions.Tests) unaffected. **This is the second time in this section a "verify the compiled artifact" check gave false confidence** (the first being 17.18c's `WelcomeDlg`-vs-`WelcomeEulaDlg` mixup) - both times the fix was to inspect data at a lower level (raw table rows, raw character codes) than the previous check had gone, not to trust that a plausible-looking rendered string meant the underlying encoding was actually correct.
- [x] 17.18f *(added)* **The dark installer theme (17.9c) was reversed on direct request** - "dark theme in WiX does not look good... let's move for a light style. icons are fine, just banner, dialog background." A significant simplification, not just a recolor:
  - **`banner-493x58.bmp`/`dialog-493x312.bmp` regenerated light** (white-to-lavender gradient, same icon mark, same generation approach as every other asset pass this session) - but positioned deliberately, not just recolored, based on where each dialog's own *native* stock text actually sits: `dialog-493x312.bmp` (used by `WelcomeDlg`/`ExitDialog`, whose native Title/Description sit at X=135..355 of 370 units, i.e. the right two-thirds) now carries the icon+wordmark in the *left* column instead; `banner-493x58.bmp` (used by `VerifyReadyDlg`/`ConfigDlg`'s new banner strip, whose native title text sits at X=15..315, i.e. nearly the full left ~85%) carries it in the *right* strip instead - "the banner up top should be light themed and icons/text in there need to be to the right (WiX will display strings to the left over that)," per direct instruction. Confirmed the two panel/dialog assets are entirely separate files from the dashboard's own `logo.png`/`favicon.ico` (installer BMPs vs. dashboard PNGs/ICO, never shared) before doing any of this, since the operator asked to confirm.
  - **`ConfigDlg`/`InvalidConfigDlg` reverted from the full-panel dark treatment (17.9c) to the standard WixUI convention** - a banner strip (the same `WixUIBannerBmp` stock dialogs use, now light) plus a plain dialog body, no custom `Background`/`TextStyle` at all. This also fixed a real, direct bug report ("the controls in the config dialog had no titles (or they were too dark to read)"): the dark theme's label `TextStyle` was near-white (`Red="230" Green="232" Blue="235"`), chosen specifically to read against the dark panel - once the panel was gone, that same near-white text on the *now-plain-white* body was just as unreadable in the opposite direction. Every label (`RepoRootLabel`/`BearerTokenLabel`/`PortLabel`) already existed as a separate control from its `Edit` field; the fix was dropping the custom style/`Transparent` attributes entirely, letting them render in the OS's normal default dark caption color, which needs no special handling against a normal light dialog body.
  - **`VerifyReadyThemedDlg` (the full hand-authored replacement from earlier this session) removed entirely**, reverting to the stock `VerifyReadyDlg` - now safe, since the underlying problem (its native black title sitting on a dark banner) no longer exists once the banner is light. This also removes the three retargeting `<Publish Dialog="..." Value="VerifyReadyThemedDlg">` overrides (`ConfigDlg.Next`, `WelcomeDlg.Next`'s patch case, `MaintenanceTypeDlg`'s Repair/Remove buttons) - `ConfigDlg.Next` still needs its own explicit wiring (retargeted to plain `VerifyReadyDlg`), but the other three no longer need any override at all, since the native stock behavior already correctly points at `VerifyReadyDlg` once nothing is diverting away from it.
  - **The three now-fully-unused `panel-*.bmp` assets and their `Binary` declarations removed** - `PanelBmp370x290`/`PanelBmp370x270`/`PanelBmp260x85` had no remaining references anywhere once the above three dialogs stopped using them.
  - **Bootstrapper (`Theme.xml`) redesigned per direct request**: "i need the version also in the bootstrapper... the very first dialog, there is bigger icon and the name of product next to it. they look unaligned. under the product name display the version in a smaller font. make that block (name + version) vertically centered to the bigger icon." The icon grew from 64x64 to 96x96 (its native `logo-96.png` resolution, avoiding upscale blur); a new smaller `Font Id="4"` (graytext, `Height="-14"`, roughly two-thirds the title's size) was added for a version line now shown via `#(loc.InstallVersion)` (`"Version [WixBundleVersion]"` - a stock loc string that was already defined but never actually referenced by any control in the theme, so `ShowVersion="yes"` had no visible effect the whole time this string existed). Both the title and version labels are now positioned as one block, vertically centered against the icon's own 96px height (icon spans Y=11..107, center Y=59; the two-line block spans Y=33..85, centered on that same midpoint) rather than each independently top-anchored the way the previous 64px-icon layout had them - which is what read as "unaligned." Every other page's own content (`Help`/`Options`/`Progress`/`Modify`/`Success`/`Failure`) needed its own Y-coordinates shifted down to clear the now-taller icon (previously starting at Y=80, now Y=118+), since all of it sits in the same `Window`-level coordinate space the icon occupies. Checked explicitly for any other "style adaptation" to the bootstrapper the operator's message referenced ("i noticed you did changes in style there - remove those adaptations, since they did nothing") - found none beyond the already-confirmed-working EULA removal (verified this session's own diff history: the top-level `ImageControl`/`Label` were byte-identical to the stock theme before this round's edits), so nothing else was reverted.

  Full test suite (124/124 UnitTests, 5/5 ArchitectureTests, 11/11 CustomActions.Tests) unaffected - WiX authoring and image-asset changes only.
- [x] 17.18g *(added)* Two more real bugs found on continued click-through, plus a cosmetic ask: (1) `ConfigDlg`'s title (repositioned onto the banner in 17.18f, matching stock placement) showed an unwanted opaque background - missing `Transparent="yes"`, which every stock title sitting on a banner bitmap carries (confirmed against `VerifyReadyDlg`'s own `InstallTitle`) and which was dropped along with the rest of the dark-theme styling in 17.18f without checking whether any of it was still needed on light backgrounds; it still was, just for a different reason (an opaque Text control paints its own background box regardless of theme, and that box is only invisible when there's no bitmap under it to show through). (2) A genuine regression: clicking Back from `VerifyReadyDlg` landed on `MaintenanceTypeDlg` ("Change/Repair/Uninstall") instead of returning to `ConfigDlg` - the standalone `<Publish Dialog="VerifyReadyDlg" Control="Back" ...>` override that made this work (present since `ConfigDlg` was first authored in 17.9) had been folded into `VerifyReadyThemedDlg`'s own Back control when that custom dialog existed, and was never restored as a standalone override when 17.18f reverted back to stock `VerifyReadyDlg` - a real gap in that revert, only caught by an operator actually clicking Back. Fixed by re-adding the override. (3) Bootstrapper version label's +3px X nudge from an earlier round was wrong per direct correction ("i was wrong on that one") - reverted, Y position unchanged. Verified against the compiled MSI: `ControlEvent` table shows `VerifyReadyDlg.Back`'s `Ordering=2 -> ConfigDlg Condition=NOT Installed` row present (via `wix msi decompile`, not just the raw table), and `ConfigDlg`'s `Title` control decompiles with `Transparent="yes"`. Full test suite (124/124 UnitTests) unaffected.
- [x] 17.18h *(added)* `ConfigDlg` layout brought fully into line with `VerifyReadyDlg`'s own conventions, per direct request: (1) left/right padding equalized for every body control - `X=25`/`Width=320` instead of `X=15`/`Width=340`, matching `VerifyReadyDlg`'s `InstallText` exactly (`370 - 25 - 320 = 25` both sides), leaving only the `Title` control flush at `X=15` the way `InstallTitle` is; (2) `Description`'s own top position moved to `Y=70`, byte-identical to `InstallText`'s own `Y=70` - the previous `Y=75` didn't match; (3) a deliberate 5px-larger gap kept between the intro block (`Title`/`UpgradeNotice`/`Description`) and the interactive fields below, with every field's position computed relative to the intro block's new anchor rather than as independently-chosen absolute values (so a future change to the intro block's position doesn't require re-deriving every field below it by hand); (4) `ConfigDlg`'s title text changed to the literal "Configure options" (dropping `[ProductName]`/`[ProductVersion]`), per direct request. Verified against the compiled MSI's `Control` table. Full test suite (124/124 UnitTests, 5/5 ArchitectureTests, 11/11 CustomActions.Tests) unaffected.
- [x] 17.18i *(added)* **The service crashing with "No server certificate was specified" (surfaced once 17.18d/e finally got a real install far enough to start Kestrel) was left as a manual post-install step per 16.2's original scope - on explicit request, the installer now generates and configures one automatically instead.** Added `CertificateGenerator` (`installer/OutOfTheBox.Msi.CustomActions`) using the in-box `System.Security.Cryptography.X509Certificates.CertificateRequest`/`SubjectAlternativeNameBuilder` APIs (confirmed available on net472 by actually building against them, not assumed) - a self-signed, 2048-bit RSA, 10-year server-authentication certificate covering the machine's hostname, `localhost`, loopback, and every "up" local IPv4 address gathered live at install time (so the cert is actually valid for whatever address a caller connects through, not just an assumed hostname). `ResolveSecrets` (the existing custom action already resolving the bearer token and service account password) now also resolves a `CERTPASSWORD` via the exact same `SecretResolution.Resolve` three-way precedence, and - only if `%ProgramData%\OutOfTheBox\outofthebox.pfx` doesn't already exist - generates and writes the PFX there, mirroring the "never rotate an already-issued secret out from under the operator" reasoning already applied to the token and account password: an upgrade must not silently invalidate a certificate an sbx caller may have already pinned. The data directory path is computed directly via `Environment.GetFolderPath(SpecialFolder.CommonApplicationData)` rather than read from the MSI's own `DATADIRFOLDER` property, because that Directory-table property isn't reliably resolved this early in the sequence (`ResolveSecrets` runs `After="AppSearch"`, well before `CostFinalize` finalizes directory paths) - computing it independently, the same way `Program.cs`'s own default already does, sidesteps that ordering problem entirely rather than fighting it. `Kestrel__Endpoints__Https__Certificate__Path`/`Certificate__Password` added to the same `Environment` `RegistryValue` as every other setting, using the same `<MultiString Value="..." />` per-entry authoring this section's own hard-won `[~]`-separator fix (17.18e) established - re-verified the same way (raw character codes, `[~]` between every entry). New `CertificateGeneratorTests` (4 tests: password round-trips correctly, wrong password is rejected, SAN covers `localhost`/machine name, validity window covers now) bring `installer/OutOfTheBox.Msi.CustomActions.Tests` to 15/15. `INSTALL.md`'s "Certificate" section rewritten to describe the new automatic behavior (and how to supply a custom certificate instead, by pre-placing a file or overriding the environment variables afterward) rather than presenting the manual `dotnet dev-certs` steps as the only path; those steps are kept for the `dotnet run`-only development flow, which has no installer to automate this for it. Verified against the compiled MSI via `wix msi decompile` (the new `Property`/`RegistrySearch` rows, and the `MultiString` entries in the `Environment` value) - actually exercising the custom action end-to-end (confirming it runs without the class of CLR-hosting failure 17.18a found) remains part of 17.18's own still-deferred real-install verification. Full test suite (124/124 UnitTests, 5/5 ArchitectureTests, 15/15 CustomActions.Tests) passes.
- [x] 17.18j *(added)* **First real dashboard verification in an actual browser against a real deployment (all prior "verified live" claims in this project were `dotnet run` on localhost) found a genuine, previously-undetected bug**: the operator reported host CPU/RAM permanently showing "0.0% / 0 B / 0 B" with a blank graph. Ruled out the server-side sampling pipeline first, methodically: `HostResourceSampler`/`HostResourceSamplerService`/`InMemoryResourceEventBus` were all read end-to-end and found correct, and critically, `BackgroundServiceExceptionBehavior` defaults to `StopHost` since .NET 6 - since the whole service stayed healthy (confirmed via `/login` and an authenticated `/run` both working), the sampler could not have been throwing, or the entire process would have crashed. Also ruled out client-specific causes (Windows Server's IE Enhanced Security Configuration) since the identical symptom reproduced in Chrome from a separate machine, not just Edge on the VM. The real evidence came from the browser's own DevTools console: the SignalR circuit connects successfully, then immediately terminates with an unhandled exception - meaning *no* live update ever reaches *any* page (confirmed separately: a run started via `curl` never appeared on the Status page without a manual refresh), not something specific to resource data. Chart.js itself was independently confirmed broken too: `Uncaught Error: This method is not implemented: Check that a complete date adapter is provided` - `chart-interop.js` uses `scales: { x: { type: "time" } }`, which Chart.js requires a separate date-adapter library for, and none was ever vendored (only `chart.umd.min.js` itself). Fixed by adding `chartjs-adapter-native.js` - a small, dependency-free adapter implementing Chart.js v4's documented adapter interface (`parse`/`format`/`add`/`diff`/`startOf`/`endOf`) using plain JS `Date` arithmetic, since the dashboard's only use of the "time" scale is a live epoch-millisecond series with no need for a full external date library (`date-fns`/`luxon`/`moment`) - consistent with this project's established preference for small hand-rolled solutions over new dependencies where the need is narrow. Wired into `App.razor` between `chart.umd.min.js` and `chart-interop.js`. Verified: full `.slnx` build and 124/5 UnitTests/ArchitectureTests pass; a local `dotnet run` smoke test confirmed the new file is served correctly (`HTTP 200`, `content-type: text/javascript`) - genuine in-browser confirmation that Chart.js accepts the adapter without throwing was **not** possible from this environment (no browser available), a real verification gap being handed back to the operator's own real-machine testing, same as every other fix in this section. **The circuit-termination bug itself remains open** - the Chart.js fix addresses a real, independently-confirmed-broken rendering path, but is not itself proven to be the *cause* of "nothing live-updates," which manifests before any chart even mounts; root-causing that is still in progress, pending the actual exception detail from the Windows Event Log (`UseWindowsService()` redirects default ASP.NET Core logging there, so no code change/redeploy should be needed to retrieve it). **Resolved**: the Event Log query came back empty (no source registration rights under the dedicated low-privilege service account, per the working hypothesis at the time), so `CircuitOptions.DetailedErrors` was temporarily enabled to surface the exception directly in the browser instead (17.18k's own diagnostic groundwork). Before that ever reproduced again, 17.18k's uninstall/reinstall certificate-password-mismatch bug was found and fixed on a separate real-machine round; on the next clean install, both the numeric tiles *and* the graph updated live with no console errors - meaning the circuit was never actually throwing on this code path once the host could start at all. The most likely explanation: earlier "verified" rounds were carrying forward inconsistent state from the multiString (17.18e) and certificate (17.18i/17.18k) bugs fixed over the course of this same section, and the "circuit terminates immediately" symptom was a downstream effect of one of those, not an independent Blazor/SignalR defect. `DetailedErrors` reverted back to its default (`false`) immediately once this was confirmed, per its own code comment's commitment - detailed exception info must not stay exposed to a browser client. Full test suite (124/124 UnitTests, 5/5 ArchitectureTests) unaffected by the revert.
- [x] 17.18k *(added)* **The service crashed outright on startup with `CryptographicException: The specified network password is not correct`, reproduced specifically after an operator *uninstalled and reinstalled* (not upgraded)** - traced to 17.18i's own "never regenerate an existing certificate" logic checking only `File.Exists`, never whether the resolved password could actually open that file. The PFX lives in the data directory (`%ProgramData%\OutOfTheBox`), which by design survives uninstall (see `Program.cs`'s `dataDirectory` comment); the password protecting it lives in `HKLM\SOFTWARE\OutOfTheBox` via `PersistedSecretsComponent`, an ordinary (non-`Permanent`) MSI-tracked registry component that a full uninstall *does* remove. A plain uninstall+reinstall therefore left the old PFX on disk with no recoverable password - `EXISTINGCERTPASSWORD`'s `RegistrySearch` came back empty, `SecretResolution.Resolve` generated a fresh random one, and `ResolveSecrets` wrote that mismatched password into `Kestrel__Endpoints__Https__Certificate__Password` without ever checking it against the file it was supposedly protecting - Kestrel then failed to load its own configured certificate at `KestrelServerImpl.BindAsync`, an unhandled exception that terminates the whole host (this is startup, not a `BackgroundService`, but the same fail-hard effect). Fixed with `CertificateGenerator.CanOpen(byte[], string)` (attempts `new X509Certificate2(pfxBytes, password)`, catches `CryptographicException`) - `ResolveSecrets` now regenerates whenever the file is missing *or* the resolved password can't open it, collapsing the uninstall/reinstall case into the same code path as a genuinely fresh install rather than trying to make the password persist through an uninstall it fundamentally can't survive (registry components and `ProgramData` data have different, deliberately-chosen uninstall lifetimes - see 17.20's still-deferred manual test). Two new tests (`CanOpen_returns_true_for_the_matching_password`, `CanOpen_returns_false_for_a_mismatched_password`) bring `CustomActions.Tests` to 17/17. Full test suite (124/124 UnitTests, 5/5 ArchitectureTests, 17/17 CustomActions.Tests) passes; real-machine re-verification (does the service actually start post-fix, after the exact uninstall→reinstall sequence that surfaced this) still pending, same deferred-verification pattern as the rest of this section.
- [ ] 17.19 Manual test: re-running the bootstrapper with a newer build takes the `MajorUpgrade` path (not a fresh install, not a failure) and preserves configuration and existing history (including resource samples, across every run kind), with `/version`/the dashboard branding reflecting the new build afterward — **deferred**, same reason as 17.18
- [ ] 17.20 Manual test: uninstalling via Programs and Features removes the service, the service account, and the install directory, but leaves the data directory (config + SQLite file) in place; reinstalling recovers prior history — **deferred**, same reason as 17.18
- [ ] 17.21 Manual test: killing the service process outright (simulated crash) results in SCM restarting it per the configured recovery actions — **deferred**, same reason as 17.18

**Revised mid-section, on explicit user request, reversing this same document's earlier "WiX rejected" decision** — see `design.md`'s Packaging/Install-upgrade-uninstall decisions for the full rationale (bundling the .NET SDK is worthwhile regardless of `Host`'s own packaging, since the service's actual purpose needs the SDK on `PATH` either way; a real MSI buys native upgrade/uninstall semantics and a proper config UI a hand-rolled script can't match).

21/26 tasks complete. What's genuinely done and verified: `installer/OutOfTheBox.Msi` (service account with a real generated/preserved password, service install/crash-recovery, firewall rule, git launch condition, major upgrade, structural data-directory protection, wildcard file harvesting, the `ConfigDlg` UI page with version display now actually reachable through the bootstrapper, a real custom action resolving the bearer token and service account password, a consistent visual brand with a human-readable display name throughout, and dark theming on the two fully-owned dialogs within native MSI's real limits) and `installer/OutOfTheBox.Bootstrapper` (chains a hash-verified .NET 10 SDK + Git for Windows ahead of the MSI, itself branded to match) both build cleanly with `dotnet build`, the MSI passes `wix msi validate`'s ICE checks, and every non-trivial piece was verified against the actual compiled artifacts, not just "the build succeeded": file harvesting via cabinet extraction, the UI dialog/branding/version-display and the secrets custom action's sequencing/properties/registry entries via the Windows Installer COM API directly, and the secrets precedence *logic itself* via 11 new xUnit tests in `installer/OutOfTheBox.Msi.CustomActions.Tests`. That verification discipline caught two real bugs before either shipped: the UI dialog silently compiling into an MSI with no Dialog table at all (Fragment dead-code elimination), and `SERVICEACCOUNTPASSWORD`/`SERVICEACCOUNTNAME`/`SERVICEACCOUNTDOMAIN` being referenced throughout `Package.wxs` but never actually defined - the service account would have been created with an undefined password, unable to log on, despite a clean build and passing ICE validation. A third bug (the secrets custom action initially preferring a stored value over an explicit operator override, backwards from what was asked) was caught by reasoning through the scenario, then given permanent regression coverage via the extracted `SecretResolution.Resolve` unit tests. `install.ps1`/`upgrade.ps1` are removed; `INSTALL.md` is rewritten for the bootstrapper/MSI flow.

What's explicitly not done, not just unchecked: a **post-install `chart.js` existence check** (folded into 17.9, small and separable, not authored); and — the more consequential open item — **whether `ConfigDlg` actually displays when installing through the bootstrapper**, given Burn's default reduced UI level for chained MSI packages and the absence of a WiX v4+ `DisplayInternalUI` equivalent. **17.18-17.21's manual tests** remain deferred for the reason they always were (real second Windows machine, operator present, real system-level state) - 17.18 is now also where that bootstrapper-UI question gets answered for real.

The main .NET test suite is unchanged by this revision (124/124 UnitTests, 5/5 ArchitectureTests, 51/51 BehaviorTests still pass) since `installer/` stays outside `OutOfTheBox.slnx`; it now has its own, separate test coverage instead (`installer/OutOfTheBox.Msi.CustomActions.Tests`, 11/11 passing) for the one piece of installer logic that actually warranted it - WiX source authoring itself still has no unit-testable surface, correctness there comes from the build/ICE-validation/table-inspection verification above and, ultimately, from 17.18-17.21.

## 18. Claude Code Skill

- [x] 18.1 Create `skills/dotnet-command-service/SKILL.md` with frontmatter (name, description) identifying it as the client guide for calling this service — `dotnet`, `git`, and artifact transfer alike
- [x] 18.2 Document authentication: bearer header name/format, where the token value comes from (the sbx-side operator's own configuration, not hardcoded), and that it's required on every endpoint below
- [x] 18.3 Document starting a `dotnet` run: `POST /run` request shape (argument list, working directory, optional timeout), reading the `X-Run-Id` response header
- [x] 18.4 Document starting a `git` run: `POST /run/git`, same request/response shape as `POST /run`, explicitly noting no subcommand or flag is restricted (including destructive ones) and that it shares the same per-repo lock as `dotnet` runs, bidirectionally
- [x] 18.5 Document requesting an artifact transfer: `POST /artifacts` (repo, file path), reading the `X-Run-Id` header before the binary body, and that a path outside the named repo is rejected — advise the caller to only request paths it already knows are within the repo it just built
- [x] 18.6 Document consuming the SSE stream from a Bash-based agent (for `POST /run`/`POST /run/git`): the `curl -N` / background-process-and-poll pattern, the `stdout`/`stderr`/`done`/`error` event types, and the `error` reasons (`validation`, `timeout`, `cancelled`); separately document that `POST /artifacts` returns a plain binary body (not SSE) and how to save it to a file
- [x] 18.7 Document cancelling any run: `POST /run/{runId}/cancel` (one endpoint for both command kinds and transfers), and what a 404 (unknown/already-finished run) means
- [x] 18.8 Document error responses relevant to the caller: 401 (missing/invalid credential), 409 (repo busy, with the blocking run's id — `dotnet`/`git` only), 404 (unknown run id on cancel, or missing file on a transfer, distinguished by endpoint context) — **corrected while writing, against the real `RunEndpoints.cs`/`ArtifactTransferEndpoints.cs` code rather than this checklist item's own wording**: a busy repo is *not* an HTTP 409 - `response.StartAsync()` commits the `/run`/`/run/git` response to `200 OK` SSE before any validation runs, so a busy repo (like every other `/run`/`/run/git` rejection) is reported as an SSE `error` event (`{"reason":"validation","runId":"<blocking-run-id>"}`) inside that 200, not a distinct HTTP status. `/artifacts`, which never starts streaming before validating, genuinely does use real HTTP status codes (400/404) as this task describes. Documenting the wire-accurate version was necessary for 18.11's "succeeds without needing to read the specs directly" bar to be achievable at all
- [x] 18.9 Explicitly scope the skill to API consumption only — no dashboard, resource-monitoring, or install/upgrade content, since those aren't for the sbx caller. **Explicitly note repository management is out of scope too**: cloning/deleting repositories has no REST endpoint at all (per `specs/repository-management`) — it's a dashboard-only, operator-triggered action the sbx caller cannot perform and this skill does not document.
- [x] 18.10 Cross-reference which spec files (`specs/dotnet-command-execution`, `specs/git-command-execution`, `specs/artifact-transfer`, `specs/service-authentication`) the skill restates, as a marker for keeping it in sync when the API changes
- [ ] 18.11 Manual test: have a real Claude Code instance follow only the skill's instructions (no other context) to authenticate, start a `dotnet` run against the passing-test fixture, start a `git` run against the git fixture, request an artifact transfer of a file the `dotnet` run produced, observe streamed output, and cancel a separate hanging run — confirm it succeeds without needing to read the specs directly — **deferred**: needs a real second Claude Code instance in a genuinely separate sandboxed context (no shared conversation history with this one) plus a reachable running `Host` instance, neither of which this session can set up unattended

Cross-checked every documented request/response shape (field names, header names/timing, SSE event framing, HTTP status codes) directly against `RunEndpoints.cs`, `ArtifactTransferEndpoints.cs`, `SseWriter.cs`, `BearerAuthenticationFilter.cs`, and `StartRunRequest`/`ArtifactTransferRequest` rather than from `design.md`'s prose alone, since this is the one artifact in the repo a caller might follow *without* also reading the source - inaccuracies here are load-bearing in a way they aren't for internal docs. No test suite changes (a Markdown skill doc has nothing to unit-test); 124/124 UnitTests, 5/5 ArchitectureTests, 51/51 BehaviorTests unchanged from Section 17.

## 19. End-to-End Verification (manual, real deployment)

- [ ] 19.1 From a real remote caller, send an authenticated `dotnet --version` request and confirm round trip
- [ ] 19.2 Run a real `dotnet build` and `dotnet test` against a sample repo through the service and confirm results match running the same commands locally
- [ ] 19.3 Run a real `git status`, `git pull`, and one destructive command (e.g. `git reset --hard`) against a sample repo through the service and confirm the repo's actual on-disk state matches what running the same commands locally would produce
- [ ] 19.4 Request a real artifact transfer of a file a prior `dotnet build` produced and confirm the received bytes match the file on disk exactly; attempt a path-escape request and confirm it's rejected without transferring anything
- [ ] 19.5 Confirm unauthenticated and path-escaping requests are rejected end-to-end, across `dotnet`, `git`, and artifact-transfer endpoints
- [ ] 19.6 Confirm parallel commands against two different repos both complete, and confirm a second command against a busy repo (of either kind, against a busy repo held by either kind, **in both directions**) is rejected with the busy run's id; confirm an artifact transfer against a busy repo is *not* rejected
- [ ] 19.7 Confirm cancelling a real in-flight run (of each of the three sbx-facing kinds) kills the process or stops the stream, ends the stream as `cancelled`, and frees the repo for a new run where applicable
- [ ] 19.8 Confirm the dashboard, opened in a real browser during runs of every kind, shows each live and then shows it correctly in history afterward, including full output (commands), file metadata (transfers), or outcome-only detail (deletes)
- [ ] 19.9 Confirm the dashboard's history filter/search controls correctly narrow a mixed set of past runs by kind, outcome, **repository**, and free text
- [ ] 19.10 Restart the service mid-run (for each run kind) and confirm the affected history row shows `Interrupted` while unrelated prior history remains intact
- [ ] 19.11 Run the full install → use → upgrade sequence end-to-end on a clean host and confirm no manual steps beyond running the two scripts were needed, and that the host's `git` prerequisite check in `install.ps1` behaves correctly both when `git` is present and when it's deliberately absent
- [ ] 19.12 During a real `dotnet test` run, confirm the dashboard shows live CPU/RAM (host and per-run process tree including `testhost.exe`), and confirm killing `testhost.exe` from the dashboard clears the hang without needing to cancel the whole run
- [ ] 19.13 During and after that same run, confirm its live graph updates while in flight and its full-duration graph is viewable afterward in history; separately confirm an artifact transfer's graph shows host-level activity (not a fabricated per-process figure) and is clearly labeled as such
- [ ] 19.14 Confirm `dotnet build` and `dotnet test` (unit + BDD) both pass on a clean checkout via the .NET 10 SDK CLI alone, with no manual setup beyond `dotnet restore`
- [ ] 19.15 Confirm a real sbx-side Claude Code instance, using only the skill from §18, successfully drives a full build/test/git/artifact-transfer/cancel cycle against a real deployed instance of the service — and confirm (by inspecting the skill and, separately, by attempting it) that repository clone/delete are genuinely unreachable to it
- [ ] 19.16 From the dashboard (not the sbx caller — repository management has no API), clone a new repository, confirm it appears live in Status and then in the Repos list with correct stats; delete a repository and confirm it's removed from disk and from the Repos list; attempt to delete a repository with an in-flight build and confirm it's rejected
- [ ] 19.17 Confirm the Repos view's filter/search controls and live active/git-status updates behave correctly against a handful of real repositories in varied states (clean, dirty, no-git, active, idle)
- [ ] 19.18 Confirm the dashboard displays "OutOfTheBox v1.0.0" (or whatever the actual published version is) consistently across Status/History/Repos and their subpages, and that it matches what `/version` reports

## 20. Project Structure Cleanup

A structural review after §8 flagged the double-nested `src/<Layer>/<ProjectName>/` and `tests/<TestType>/<ProjectName>/` layout as non-conforming with common .NET practice (Ardalis'/Jason Taylor's Clean Architecture reference layouts keep `src/` flat), plus three missing common-practice files. `global.json` was explicitly excluded from this cleanup.

- [x] 20.1 Flattened all five `src/` projects: `git mv src/<Layer>/OutOfTheBox.<Layer> src/OutOfTheBox.<Layer>`, removed the now-empty layer directories
- [x] 20.2 Flattened all three non-fixture `tests/` projects the same way (`tests/<TestType>/OutOfTheBox.<TestType>` → `tests/OutOfTheBox.<TestType>`); `tests/Fixtures/` untouched (categorically different - external-repo simulators, never solution-registered)
- [x] 20.3 Rewrote `OutOfTheBox.slnx`: removed the five `Domain`/`Application`/`Infrastructure`/`Presentation`/`Host` solution folders (projects now sit flat at the solution root); `Tests` remains the only solution folder, now wrapping flat (unfoldered) test projects; updated every `<Project Path>` to the new flat paths
- [x] 20.4 Fixed every `<ProjectReference>` path in all 7 affected `.csproj` files by hand (one directory level shallower each; recomputed rather than round-tripped through `dotnet remove/add reference` to avoid touching unrelated formatting)
- [x] 20.5 Added `Directory.Packages.props` (Central Package Management) at the repo root: all 9 package IDs used anywhere in the repo declared once as `<PackageVersion>`; stripped the `Version` attribute from every `<PackageReference>` in all 9 affected `.csproj` files, including the three `tests/Fixtures/` projects (CPM applies by directory walk-up regardless of `.slnx` membership)
- [x] 20.6 Added `.editorconfig` at the repo root: charset/EOL/indentation basics plus a handful of C# style conventions already in use throughout the codebase (file-scoped namespaces, `var` preference, `_camelCase` private fields) — all at `:suggestion` severity, since `CS1591` already covers the one rule that needed to be build-breaking
- [x] 20.7 Updated `openspec/changes/sbx-dotnet-command-service/{proposal,design}.md` to describe the flat layout, and to add the CPM/`.editorconfig` decisions; updated `README.md`/`BUILD.md`/`CLAUDE.md` path references; added a `CHANGELOG.md` entry
- [x] 20.8 Confirmed clean `dotnet build`/`dotnet test` after every step (flattening, reference fixes, CPM, editorconfig) — 58/58 tests passing throughout, plus a standalone build check that a `tests/Fixtures/` project still restores correctly under CPM despite not being solution-registered
- [x] 20.9 Adopted the .NET SDK's centralized artifacts output layout: `UseArtifactsOutput` + an explicit `ArtifactsPath` (`$(MSBuildThisFileDirectory)artifacts`) in the root `Directory.Build.props` — the explicit pin was necessary because `tests/Directory.Build.props` (a second `Directory.Build.props` file) otherwise anchors the SDK's default `ArtifactsPath` inference to `tests/` instead of the repo root, splitting output across two locations instead of unifying it
- [x] 20.10 Deleted all eleven pre-existing per-project `bin`/`obj` directories (five `src/`, three `tests/`, three `tests/Fixtures/`) and rebuilt clean; confirmed every project (including `tests/Fixtures/`, via the same directory-walk-up behavior CPM already relies on) now lands under `artifacts/bin/<ProjectName>/debug/` and `artifacts/obj/<ProjectName>/`
- [x] 20.11 Added `artifacts/` to `.gitignore` (kept `bin/`/`obj/` too, as a safety net); updated `BUILD.md` (new "Build output" section), `CLAUDE.md` (new Conventions bullet), and `design.md` (new Decision) to document the layout and why `ArtifactsPath` had to be pinned explicitly
- [x] 20.12 Confirmed clean `dotnet build`/`dotnet test` after the change — 58/58 tests passing, all under the unified `artifacts/` layout
