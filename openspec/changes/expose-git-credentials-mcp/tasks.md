## 1. Domain/Application types

- [ ] 1.1 Add `GitHostAuthorization(string Host, DateTimeOffset AuthorizedAtUtc)` in `OutOfTheBox.Domain.Repositories` (or a new `OutOfTheBox.Domain.GitCredentials` namespace if that reads cleaner) - never includes the token itself, and no username field (PAT-only, per design.md's "no username parameter" decision).
- [ ] 1.2 Add `IGitCredentialStore` port in `OutOfTheBox.Application` with `AuthorizeAsync(string host, string token, CancellationToken)`, `ListAuthorizedHostsAsync(CancellationToken)` (returning host + `GitHostAuthorization`/`GitHostCredentialHealth` joined), `RevokeAsync(string host, CancellationToken)` returning whether anything was actually removed.
- [ ] 1.3 Add `ProcessRunRequest.StandardInput` (`string?`, default `null`) - purely additive, every existing caller unaffected.
- [ ] 1.4 Add `GitAuthFailureClassifier` (`OutOfTheBox.Domain`, pure function over a nullable stderr string) - `IsLikelyAuthFailure(string? stderr)`, pattern-matching git's known auth-failure message shapes (`Authentication failed for`, `Invalid username or password`, `could not read Username for ... terminal prompts disabled`, HTTP 401/403 wording). Conservative by design - a miss degrades to today's generic-failure behavior, never a false positive that triggers an unwarranted PAT prompt.
- [ ] 1.5 Add `GitRemoteUrlParser.TryGetHost(string url, out string host)` (`OutOfTheBox.Domain`, pure string parsing) - handles both `https://host/...`/`http://host/...` (via `Uri.TryCreate`) and the SCP-like `user@host:path` form.
- [ ] 1.6 Add `GitHostCredentialHealth(string Host, DateTimeOffset? LastAuthFailureAtUtc, DateTimeOffset? LastAuthSuccessAtUtc)` in `OutOfTheBox.Domain.Repositories` - separate from `GitHostAuthorization` (see design.md's "needs-credential tracking is a separate type" decision). Add a Domain-level `NeedsCredential(this GitHostCredentialHealth?)` (or equivalent) helper: true iff a failure timestamp exists and is not older than the success timestamp.
- [ ] 1.7 Add `IGitCredentialStore.RecordOutcomeAsync(string host, bool succeeded, CancellationToken)` (upserts `GitHostCredentialHealth` for that host) and `GetHealthAsync(string host, CancellationToken)`.
- [ ] 1.8 Add `RepositorySummary`/`GitStatusSnapshot`'s `NeedsCredential: bool` field (`OutOfTheBox.Domain.Repositories`).

## 2. Infrastructure: process runner stdin + credential store

- [ ] 2.1 `CliProcessRunner`: when `StandardInput` is supplied, write it to the child process's `StandardInput` stream and close it before awaiting output/exit.
- [ ] 2.2 New `GitCredentialStore : IGitCredentialStore` (`OutOfTheBox.Infrastructure.Repositories`): `AuthorizeAsync` runs `git credential approve` with `protocol=https\nhost={host}\nusername={fixed placeholder constant}\npassword={token}\n\n` piped via the new stdin support (fixed placeholder, not caller-supplied - see design.md), then immediately runs `git credential fill` for the same host and confirms a `password=` line comes back (discarded, never persisted/logged) before reporting success; on no verification, throws/reports a specific "could not verify" failure. `RevokeAsync` runs `git credential reject` with `protocol=https\nhost={host}\n\n`.
- [ ] 2.3 Before attempting `approve`, check `git config --get credential.helper` returns non-empty; if not, fail with a specific "no credential helper configured" error rather than attempting the write.
- [ ] 2.4 `GitHostAuthorizationRepository`/`GitHostCredentialHealthRepository` (or fold into one repository class if that fits better) backed by `OutOfTheBoxDbContext` - `Add`/`List`/`Remove` for `GitHostAuthorizations`, upsert/get for `GitHostCredentialHealth`. New EF Core migration covering both tables.
- [ ] 2.5 `RepositoryManager.PullAsync`/`PushAsync`/`ForcePushAsync`/`FetchAsync`: after each completes, resolve the repository's `origin` host and call `RecordOutcomeAsync` classifying the result via `GitAuthFailureClassifier`. `CleanAsync` untouched (no network).
- [ ] 2.6 `RepositoryManager.CloneAsync`'s underlying run: on terminal completion, resolve the host from the clone's own source URL and call `RecordOutcomeAsync` the same way. (Likely lands wherever the clone `Run`'s terminal state is already handled, alongside existing history-recording logic.)
- [ ] 2.7 `GitRepositoryStatsProvider`: derive `NeedsCredential` from `origin`'s resolved host (via the already-fetched `remote -v` output + `GitRemoteUrlParser`) and `IGitCredentialStore.GetHealthAsync`, folded into the existing per-repository sampling pass - no new network call.

## 3. Presentation: MCP tools

- [ ] 3.1 Add `GitCredentialsMcpTools.cs` (`OutOfTheBox.Presentation.Mcp`): `authorize_git_host`, `list_authorized_git_hosts`, `revoke_git_host_authorization`. Same `[McpServerToolType]`/`[McpServerTool]`/`[Description]`/`McpException` conventions as `FileManagementMcpTools`. Result records near the tool class, matching existing precedent.
- [ ] 3.2 Confirm the token parameter is never included in any logged message, exception text, or result payload - grep the finished implementation for the parameter name to verify no accidental interpolation into a log/exception string.
- [ ] 3.3 Wire `GitAuthFailureClassifier` into `git_run`'s and `clone_repository`'s MCP-facing failure-result mapping (`CommandExecutionMcpTools`/`RepositoryAccessMcpTools`): on a failed run whose stderr classifies as an auth failure, resolve the target host (repository's `origin` remote for `git_run`, the supplied source URL for `clone_repository`) and look it up via `IGitCredentialStore.GetHealthAsync`/`ListAuthorizedHostsAsync` to append the specific "never authorized" vs. "previously authorized, may have expired" note per `mcp-git-credentials`'s new requirement. Read-only against credential state - never calls `RecordOutcomeAsync` itself (see design.md's Non-Goals). A non-matching failure is untouched.
- [ ] 3.4 `RepositoryAccessMcpTools.ListRepositoriesAsync`'s result mapping: include the new `NeedsCredential` field from `RepositorySummary`, per `mcp-repository-access`'s updated requirement.

## 4. Presentation: Dashboard

- [ ] 4.1 Add `PatPromptDialog.razor`: `ShowAsync(string host, string? initialMessage = null)` returning once the operator saves a credential that verifies (via `IGitCredentialStore.AuthorizeAsync`, injected directly per this codebase's existing "Blazor component calls an Application port directly" precedent) or cancels; a failed verification re-shows the same dialog with an explicit "that token didn't work" message and an empty field, looping internally.
- [ ] 4.2 `CloneDialog.razor`: on `Accepted`, do not close - subscribe to `IRunEventBus` filtered to the returned run id (mirroring `Repositories.razor`'s own subscription) and wait for a terminal state. Completed → close + `OnCloned`, as today. Failed + `GitAuthFailureClassifier` matches → resolve the host via `GitRemoteUrlParser` from the submitted URL, show `PatPromptDialog`, and on a verified token resubmit the identical clone (loop). Failed + no match → show the failure detail in-dialog (a real gap fix - today the dialog has already closed by the time a non-preflight clone failure occurs). Cancelled via the PAT prompt → close the dialog entirely, no further retry.
- [ ] 4.3 `RepositoryQuickActions.razor`: feed `RepositoryGitActionResult.Failed(ErrorMessage)`'s message into the flashed icon's `title` attribute (currently discarded); when `GitAuthFailureClassifier` matches, use a specifically-worded tooltip pointing at the new change-credential action instead of raw stderr. No popup - passive tooltip only, per direct instruction.
- [ ] 4.4 `RepositoryQuickActions.razor`: add a "change credential" icon button (new vendored icon - see 4.6) alongside the existing five. On click, resolve the repository's `origin` remote host via `IRepositoryManager`'s existing remote-listing + `GitRemoteUrlParser`; if resolvable, show `PatPromptDialog` for that host; if not, report that no host could be determined (no dialog opens with nothing to save against).
- [ ] 4.5 `Repositories.razor`'s Name column and `RepositoryDetail.razor`'s heading: render the same vendored icon next to the repository's name when `RepositorySummary.NeedsCredential` is true.
- [ ] 4.6 Vendor a "key" (or similar) Lucide SVG into `wwwroot/icons/`, per the existing icon-vendoring convention `Icon.razor` already reads from at runtime - shared by the change-credential action (4.4) and the name marker (4.5).

## 5. Docs

- [ ] 5.1 `About.razor`: tool list grows to seventeen entries (add `authorize_git_host`, `list_authorized_git_hosts`, `revoke_git_host_authorization`), update the tool-count wording.
- [ ] 5.2 `CHANGELOG.md`: new entry under Added.
- [ ] 5.3 `INSTALL.md`: note the Git Credential Manager / `credential.helper` prerequisite explicitly if not already implied by the existing Git for Windows prerequisite text.

## 6. Tests

- [ ] 6.1 Unit tests for `GitCredentialStore` against a real temp `HOME`/`GIT_CONFIG_GLOBAL`-scoped git config with a test-only credential helper (e.g. a trivial `store`-backed helper writing to a temp file) so `approve`/`fill`/`reject` are exercised for real without touching the real Windows Credential Manager or any real host.
- [ ] 6.2 Unit tests for `GitAuthFailureClassifier` (known auth-failure phrasings match, unrelated failures don't) and `GitRemoteUrlParser` (HTTPS/HTTP URLs, SCP-like `user@host:path`, malformed input).
- [ ] 6.3 Unit tests for `GitHostCredentialHealth`'s needs-credential derivation (failure-only, success-only, failure-then-success, success-then-failure, neither recorded) and `RecordOutcomeAsync`'s upsert behavior.
- [ ] 6.4 `McpGitCredentials.feature` (`tests/OutOfTheBox.BehaviorTests/`) + steps: scenarios transcribed from `mcp-git-credentials/spec.md`'s `#### Scenario:` blocks, against a real running `Host` with the same test-scoped credential helper as 6.1, including the `git_run`/`clone_repository` auth-failure-enrichment scenarios.
- [ ] 6.5 Extend `McpRepositoryAccess.feature`/`RepositoryManagement.feature` (whichever already covers `list_repositories`/pull/push/clone) with scenarios for the needs-credential field/marker: a host with a recorded failure and no later success shows it, a subsequent success clears it, `git clean` never sets it.
- [ ] 6.6 bUnit tests for `CloneDialog`'s new retry state machine (completed/non-auth-failed/auth-failed/cancelled paths) and `RepositoryQuickActions`' new tooltip/change-credential button/name-marker rendering, following this project's existing `FileTreeComponentTests`-style fake/double conventions.
- [ ] 6.7 Update `McpServer.feature`'s "Listing available tools" scenario to the full seventeen-tool list.
- [ ] 6.8 Run `dotnet test tests/OutOfTheBox.UnitTests`/`tests/OutOfTheBox.ArchitectureTests` after Sections 1-3, again after Section 4.
- [ ] 6.9 Run the full suite including `tests/OutOfTheBox.BehaviorTests` before the final commit.

## 7. Live verification

- [ ] 7.0 Confirm all three open assumptions from design.md before relying on the rest of this section's results: (a) `authorize_git_host` against a real `github.com`/`dev.azure.com` PAT actually gets picked up transparently by a subsequent `git_run`/`clone_repository`, not overridden by Git Credential Manager's own provider-specific OAuth behavior for those two hosts - if it is, apply the `credential.https://<host>.provider generic` override alongside the credential and re-verify; (b) the above holds when run as the real installed Windows Service under its dedicated service account, not just an interactive `dotnet run` - if Windows Credential Manager storage doesn't behave correctly for that account, evaluate switching to GCM's file-based `store` helper for this service specifically; (c) the fixed placeholder username `GitCredentialStore` sends is actually accepted by both hosts (expected, per GitHub's own `x-access-token` convention and Azure DevOps' documented "username can be anything" behavior, but not yet tested against a real request) - if either host turns out to validate/reject on username content specifically, this is the point to discover and fix it, not an assumption to carry into production unverified.
- [ ] 7.1 On a real machine with Git for Windows installed, call `authorize_git_host` with a real PAT against a real GitHub (or Azure DevOps) host, then confirm a `git_run clone`/`git_run fetch` against a private repo on that host succeeds with no other credential configured.
- [ ] 7.2 Call `list_authorized_git_hosts` and confirm the host appears with a timestamp, its current health, and no token value anywhere in the response.
- [ ] 7.3 Call `revoke_git_host_authorization`, then confirm the same private-repo git operation now fails with a normal git auth error carrying the "previously authorized, may have expired" note, and the host no longer appears via `list_authorized_git_hosts`.
- [ ] 7.4 Confirm the missing-credential-helper and unreachable-git.exe error paths each produce a distinct, actionable message.
- [ ] 7.5 From a real running dashboard, clone a private repository with no credential configured - confirm the PAT prompt appears, an invalid token re-prompts with the "didn't work" message, a valid token retries and completes the clone, and cancelling the prompt leaves no repository behind.
- [ ] 7.6 Trigger pull/push/fetch against a repository with an invalid/expired credential from the dashboard - confirm the action flashes failed with no popup, and the failure tooltip shows an authentication-specific message.
- [ ] 7.7 Use the new change-credential action from both the Repositories list and repository detail page against a repository with a resolvable `origin` host, and against one with none, confirming both behave per spec.
- [ ] 7.8 With a repository's credential broken, confirm the needs-credential symbol appears next to its name in both the list and detail page, and confirm it via `list_repositories`' own result too; then fix the credential via a successful pull/push/fetch and confirm the symbol clears in all three places.

## 8. Wrap-up

- [ ] 8.1 Before the final commit, check `git diff --staged` for leftover debug code (especially anything that could echo a token), per this repository's standing convention.
- [ ] 8.2 Commit and push. Do not archive this change as part of implementation - archiving is a separate, deliberate step.
